# 贡献指南

## 快速开始

1. Fork 本仓库；
2. 创建分支：`git checkout -b feat/your-change`；
3. 提交时使用 `git commit -s`（Developer Certificate of Origin）；
4. 提交 PR（模板见 `.github/PULL_REQUEST_TEMPLATE.md`）。

### 提交前必跑：一致性校验（pre-commit）

本仓库自带发布一致性校验（`scripts/verify_consistency.py`，41 项：表格格式 / 证据分级数量（A–G 五级，含 G 类管辖前缀）/ 事故数字口径 / 章节号 / 术语口径 / 双向引用检查 / 引用键质量 / 核验日志键集同步 / 法域枚举一致性 / demo 断言声明与实际运行输出一致性 / 纠错登记时效）。启用方式（仓库根目录执行一次）：

```sh
git config core.hooksPath .githooks
```

启用后每次 `git commit` 自动执行，校验失败即拦截提交。也可手动运行：

```sh
python scripts/verify_consistency.py
```

### 提交后自动打 tag（post-commit，v2.15.6 起）

同一 `git config core.hooksPath .githooks` 已一并启用 post-commit 钩子。触发条件（同时满足才触发）：

1. 本次 commit 修改了 `VERSION` 文件；
2. `VERSION` 内容形如 `X.Y.Z`（语义化版本三段数字）；
3. 尚不存在同名 tag（`vX.Y.Z`）。

行为：从 `CHANGELOG.md` 抽取对应版本条目作为 tag message，建 annotated tag 锚定到当前 commit。**目的**：杜绝「VERSION 已更新但 tag 忘打」的版本号与 commit 脱锚事故（v2.15.6 之前所有 tag 均需手动打，偶发遗忘）。调试场景跳过自动建 tag：`SKIP_AUTO_TAG=1 git commit ...`。

## 硬性要求

### 1. 证据分级（每条机制必须）

新增或修改任何机制，必须声明证据等级：

- **A**：真实环境复现、可复验；
- **B**：可核查期刊/预印本/一手公开事件（附 arXiv/DOI/URL）；
- **C**：域迁移缺口或二手转述（须配 A/B 升级）；
- **D**：推演——**不得单独支撑 P0 机制**。

来源必须写入 `REFERENCES.md`。

### 2. 引用利益关联披露

新增文献时须同时声明：
- 作者机构性质（学术 / 商业）；
- 是否存在专利或商业利益关联；
- 关键数据是一手实证还是二手统计。

可检索到 ≠ 可作为独立证据。

### 3. 回灌检查（PR 必勾）

- ① 相邻段落同步（原则段 ↔ 执行段）；
- ② 相关表格同步；
- ③ 进度表与成熟度同步；
- ④ 变更日志与未解清单同步。

### 3.5 流程纪律（人工步骤前强制过一遍；v2.44.0 起）

> 依据：判据为「规则性特征（必须/禁止）且无法具体化为脚本的纪律，同样进 harness」。能脚本化的已进门禁（如第 34 项全量登记覆盖）；以下是暂无法脚本化的剩余项——**执行人工步骤前逐条自检**：

- ① **登记先行**：任何新增 REFERENCES 条目（不分等级）必须同步在 `VERIFICATION-LOG.md` 登记核验状态（可脚本化部分已由门禁第 34 项强制，此处核对登记内容的**语义质量**：核验方式与一手来源描述是否真实完整）；
- ② **双语语义同步**：spec 变更的 .en 镜像不只要"文件存在"，要逐段核对语义等价（门禁无法校验翻译质量）；
- ③ **计数语义复核**：门禁锁定的计数之外，正文叙述中引用的数字（如"连续 N 版无回归"）须与仓库实况一致；
- ④ **评审纪律**：吸收外部意见前先独立核验事实，不盲从单一信源；驳斥与采纳同样留痕。
- ⑤ **核验时效巡逻**（v2.44.2 起）：每次文献巡逻须以 `VERIFICATION-LOG.md`「核验日期」最早的前 20 条为固定输入复核（B / C 级预印本 6 个月到期必查；G 类遇法源修订 / 事件续报重验）；吸收巡逻结果时同步更新所涉条目的核验日期列——引用被撤回 / 证伪走证据修正流程，删除需举证。

### 4. 保密分级确认

- 不引入受限级内容（蜜罐样本构造、落地路线时间表、审计日志格式）；
- 不确定时先读 `CLASSIFICATION.md`。

## 版本管理政策（Versioning Policy）

自 v2.11.1 起生效（v2.41.0 修订），由维护者在发布时执行。本项目遵循语义化版本（`major.minor.patch`）：

- **major（架构）**：治理架构层级变更（L0–L3 定义调整）、证据分级体系（A/B/C/D/G）结构性调整、破坏性规范变更（须同步在 `BREAKING_CHANGES.md` 登记）；
- **minor（实验与机制）**：新增或升级 **A 级 / G 级**证据条目（预注册实验、演示级闭合）、spec 机制 / 章节实质变更、门禁规则变更、本版本管理政策自身的修订；
- **patch（订正与吸收）**：**B / C 级**证据条目的新增、升级与订正（含文献巡逻吸收）、计数修正、日期 / 笔误订正、双语同步、CI 等工程基建、无机制语义的文本改动；
- **打包发布**：同一批次内多类变更合入**一次**发布，版本号取其中最高档；连续小步 commit 不逐个发版——版本号只承载变更性质，不承载发布频率，变更密度以 `CHANGELOG.md` 条目为准；
- 每次发布必须同步 `VERSION` / `CHANGELOG.md` / README 双语版本徽章 / `CITATION.cff`，并**打 annotated git tag（`vX.Y.Z`）**；
- 发布前必须通过全部一致性校验（见上文），并确保 CI（`.github/workflows/ci.yml`）全绿。

背景教训（v2.11.1 初版）：v2.4.1 曾在 README 出现而仓库历史从未存在（笔误）；早期计数修正多次占用 minor 号导致版本膨胀。本政策把「计数修正 / 文本订正」显式归入 patch，并强制每次发布打 tag，杜绝「版本号与 commit 无锚点」再次发生。

修订背景（v2.41.0，外部审查 P2）：外部审查指出一个月内 v2.0 → v2.38 的发布节奏使版本号失去信息量。根因有三——「新增证据条目」不分等级全占 minor（文献巡逻一批吸收即多次 minor）；major 无定义；无打包发布指引。修订后：文献吸收（B/C 级）降入 patch，A/G 级实验维持 minor，major 显式锚定架构类变更，并引入「同一批次合入一次发布」规则。政策不回溯改判历史版本。

## 可拒绝执行的边界与凭证锚点

本指南为流程性文档，自身不另设拒绝条款：贡献者若认为某项要求在本仓库语境下不可执行，可引用 `spec/01`「可引用拒绝依据」原则（元层级判据源，登记于 spec/13 未解问题册）说明理由并提出豁免请求。文档中引用外部事实的段落须按 `spec/01` 原则二携带凭证锚点——就近锚点须可追溯至 REFERENCES 键 / arXiv 号 / DOI / spec/13 编号四者其一（字面 DOI 不作硬性要求，裁决理由见 `spec/13` #37）；指向本仓实现的声称以仓内路径或 AOE-* 实验登记号为锚点。（v2.51.1 补，闭环 #37 残余项）

## 风格

见 `STYLE.md`（术语表、编号规则、写作规范）。
