# 文档风格指南

## 术语表

| 术语 | 定义 |
|---|---|
| 一号 | 执行方 AI Agent |
| 二号 | 监察方 AI（语义层） |
| 三号甲 | 出题 / 蜜罐注入方（独立于三号乙） |
| 三号乙 | 证据固化 / 哈希链维护方 |
| L0–L4 | 任务准入 → 内生对齐 → 运行监察 → 独立验证 → 问责演化 |
| G0–G4 | 任务风险等级 |
| S0–S3 | Agent 规模等级 |
| E1–E5 | 证据完整性子系统组件 |
| 交白卷 | 合规地承认做不到（Voluntary Abstention） |
| 影子比 | 同任务跨模型/实例的消耗分布比较 |
| 蜂群 / 狼群 | 中心化 / 去中心化多 Agent 组织形态 |

## 编号规则

- 机制编号：L0–L4、G0–G4、S0–S3、E1–E5 固定不变，**不得复用或重排**。
- 未解问题编号：`spec/13-boundaries.md` 中的 #1–#26，新增从 #27 起，**不得删除或改号**。
- 引用格式：引用其他章节用概念名（如「证据完整性子系统」）而非章号；引用文献用 arXiv/DOI 号。

## 引用键选取标准

`REFERENCES.md` 每条条目都要有一个**登记键**，门禁据此判断该条目是否真的被正文引用。键的选取规则：

1. **有 arXiv 号者以号为键**，无需额外声明；**无号者必须在文献列显式声明 `【键: XXX】`**（官方博客、技术报告、期刊论文等均属此类）。
2. **键是人对引用关系的声明，不是脚本可推断的属性。** 曾两次尝试以自由关键词判定命中，两次都是误报：正文里的「EU AI Act」指向的是 Janssen 与 Nannini 而非 Mahmutovic 那篇；「Hugging Face」的 5 处命中都是把它当事故当事方提起，而非引用其技术时间线。**凡正文会泛指、或同一词在正文兼指当事方与文献的，都不能当键。**
3. **优先取第一作者姓氏或该文献独有的词元**（如 `Teichmann`、`Buscemi`、`HarnessCard`、`GEN-RWD`）；文档自身造的专有名词亦可（如 `Glasswing`、`898 题`）。
4. **键须全局唯一**，不同条目不得共用；长度 ≥ 4 字符；不得使用机构名、通用概念词与泛指词（黑名单见 `scripts/verify_consistency.py` 的 `GENERIC_KEYS`）。
5. **键一旦取定即视为该条目在正文中的引注标识**：回灌时正文必须出现该键，否则门禁按悬空处理。

## 写作规范

- 叙事主语：面向外部读者。「本框架」「本体系」可用于自指；**禁用**内部版本号（v1/v2 等）与内部审阅编号（P0-1/P0-2/P0-3 等）。允许使用公开语义的优先级标记（P0/P1/P2）与未解问题编号（C12/C13/C14 等）——前者表示关键程度，后者是未解清单条目号，均非内部审阅痕迹。
- 证据要求：任何事实断言必须可追溯至 `REFERENCES.md`；推演必须标注「推演」。
- 中文写作：优先使用文档中定义过的术语；首次使用全称，后续可用简称。
