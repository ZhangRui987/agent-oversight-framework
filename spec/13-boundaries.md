# 仍未解决的问题（诚实边界）

本体系设计了三块核心能力，但没有、也无法解决所有问题。以下问题明确登记，不做粉饰。
| 序号 | 未解问题 | 状态 | 下一步 |
| --- | --- | --- | --- |
| 1 | 蜂群归因的粒度：当指挥体系自发涌现而非显式设计时，「谁是发起者」在哲学上可能没有答案 | 未解 | 需引入混合裁决机制 |
| 2 | 证据层的递归：E1 内核侧采集点自身的完整性由谁保证？若内核被攻陷，E 子系统同样失效 | 部分解（E5 缓解） | 物理断点：独立硬件域 + 远程证明 |
| 3 | 善意蜂群与恶意蜂群的区分：大规模协作本身是 Agent 系统的价值，误判会扼杀正当的多 Agent 应用 | 未解 | 需在真实多 Agent 业务上标定阈值。追加约束：标定完成前 S3 不得启用硬熔断，仅记录与告警（见 7.3 阈值标定前置条件） |
| 4 | 探索性任务边界：G0 之外的适用边界仍需个案演化 | 未推进 | 建议「创新豁免申请制」：明确探索边界、风险预案、事后复盘 |
| 5 | 主指标 M 的量纲问题：M1（委托成功率差，[-1,1]）与 M2（被迫采用比例，[0,1]）不可通约，相乘无自然解释 | 未解 | 改为 M1 / M2 分别独立报告，或改用分层对比、准实验设计 |
| 6 | A2 监察强度维度降维（三维降为二维） | 未处理 | 需在配套细则中回应 |
| 7 | 保密分级的落地时间表：分级表已给，但主进度表中无对应交付项 | 未排期 | 需在 P1 阶段补入交付项 |
| 8 | 资源账本、算力隔离、出站量级拦截三项 P0 机制的原始支撑来源已撤销，现由替代来源支撑 | 已替代 | 若替代支撑不足，应诚实降为 P1 而非维持 P0 虚名 |
| 9 | C12 信用分计算公式：为本体系 P0 级机制空缺 | 未解（登记） | P1 给出量规草案：定义存储位置、更新频率、半衰期算法、跨任务迁移规则、低信用强制重训触发条件 |
| 10 | C13 任务可验证性评估协议：为本体系 P0 级机制空缺。它是 G0–G4 × S0–S3 分级体系的输入判据 | 未解（登记） | P1 给出协议草案；在 C13 落地前，分级定级须由人类显式裁定，不得自动化 |
| 11 | C14 关键操作定义：为本体系 P0 级机制空缺。看门狗的确定性拦截规则清单依赖此定义 | 未解（登记） | P1 给出清单；在 C14 落地前，看门狗仅覆盖已明确列举的高危操作，不做兜底推断 |
| 12 | A4 三号拆分：出题者同时固化证据则独立性不成立，要求拆分 | 已解 | 9.4 拆为三号甲（出题/蜜罐）与三号乙（证据固化），分属不同信任域与运维主体 |
| 13 | A8 规划—执行分离（二号主导拆解、一号纯执行）：候选架构 | 候选架构 | 不在当前版本落地；若未来落地须先解决与 7.2 指挥集中度阈值的兼容问题 |
| 14 | 三份一手材料链接：已补入 REFERENCES.md | 已解 | 已解：三份一手材料（OpenAI 官方博客 / 38 页技术报告 / METR & Redwood 独立调查）的正式链接已补入 REFERENCES.md；另补入 HF 受害方技术时间线。 |
| 15 | 去中心化蜂群（狼群）的反制手段缺失：7.2 已能检测（新增「协同效能异常」信号），但 7.3 分片 / 降级 / 降速的效果仅对中心化蜂群有依据；MultiAgent4Collusion（arXiv:2507.14660）实证显示狼群在社交与电商场景的作案效果一致优于中心化军队，且分片后仍可靠局部信息继续协同 | 未解（工程缺口，非认识论限制） | P2 完成候选反制方向的评估与选型。验收标准：至少一条方向在可复现的仿真环境（可用 MultiAgent4Collusion 复现）上给出可测量的协同效能抑制率。候选方向（均为 D 级推演，未经证实）：A 能力包络强制降维到个体层——不追问「谁是主谋」，改对每个 Agent 单独检测行为分布偏移，使处置不依赖归因，从而绕开 #1 的哲学死结；B 对 Agent 间通道单独计量与配额，使协同的边际成本随规模超线性上升，可复用资源账本；C 攻击收敛条件而非连通性——限制共享记忆写入频率、引入读延迟、压缩可观察的同伴样本量，使协同收敛时间超出任务窗口；D 污染共享知识（注入误导信息）建议列为禁区——技术上有效，但与 E 子系统「证据不可伪造」的核心伦理直接冲突。法学支撑：Teichmann（Law Innovation and Technology, 2026, DOI 10.1080/17579961.2026.2718578）论证 agentic AI 使「溯源」与「归责」合并、三范式无一单独足够，主张以部署方尽职调查与可追溯性为锚的分布式问责——与本方向 A「处置不依赖归因」同构。 |
| 16 | harness 自修改的准入门槛与回归容忍上限未标定：架构章已规定修改权归属、门槛分层与准入判定式（g(c) > 0 且 R(c) ≤ δ），但 **δ 的取值、变更风险的三级划分、以及「高风险变更需要更强准入门槛」中「更强」具体指什么，均无经验依据** | 未解（新登记） | 需在真实业务上按变更类型分别标定 δ；标定完成前，对 harness 与看门狗规则的自动修改一律按最高风险档处理（禁止自动写入生产） |
| 17 | 自动分级依赖元数据可信度，而元数据可信度本身没有保障：分级路由的失效保护只在「元数据正确」或「不确定性被检出」两种情形下成立，**自信但错误的元数据会击穿它**。即本体系的自动化机制（含 G×S 定级与资源账本分层采集）在输入端继承了一个未被管辖的风险 | 未解（新登记，吸收自外部文献的残余风险清单） | 元数据可信度应显式纳入证据完整性子系统（E 子系统）的管辖范围，而非假定其为真；在元数据可信度验证机制落地前，自动分级的输出须标记为「待核定」，不得直接作为处分依据 |
## 接入前置披露：harness 披露卡
本体系多处要求「部署方自评」「采集点说明」「规则可 diff」，但一直缺少统一的输入格式——条款只能靠问，无法靠查。补一道前置要求：任何系统接入本框架前，须提交一份 harness 披露卡，把「模型之外的那一层究竟是什么」讲清楚。
可直接采用现成工件：Harness Engineering for Language Agents (CAR)（Preprints.org，DOI 10.20944/preprints202603.1756.v1）提出的 **HarnessCard**，共 11 个字段（9 项必需、2 项推荐），已逐字核对于该文附录 Table A5：

| # | 字段 | 应披露什么 | 要求 |
| --- | --- | --- | --- |
| 1 | Base model(s) | 模型名称、版本、解码设置，以及任何微调或适配器 | 必需 |
| 2 | Control artifacts | 系统指令、AGENTS.md 类文件、仓库地图、架构规则、模式定义、测试、linter、完成判据 | 必需 |
| 3 | Runtime policy | 记忆类型、压缩或摘要策略、检查点、重试与回滚策略、预算限制 | 必需 |
| 4 | Action substrate | 工具、API、浏览器或 GUI 访问、代码执行、接口模式、MCP 使用 | 必需 |
| 5 | Execution topology | 单 Agent 还是多 Agent 结构、规划者 / 验证者角色、复核回路、路由逻辑 | 必需 |
| 6 | Feedback stack | 测试、评分器、反思提示、隐藏检查、人工干预、修复回路 | 必需 |
| 7 | Governance layer | 权限、沙箱、升级规则、策略检查、来源日志、审计支持 | 必需 |
| 8 | Observability | 存储的轨迹、重放支持、延迟与成本日志、失效分类 | 必需 |
| 9 | Evaluation protocol | 任务集、运行次数、成功判据、方差处理、留出检查或预算限制 | 必需 |
| 10 | Release artifacts | 提示词或程序、工具规格、轨迹、配置、环境搭建、可复现说明 | 推荐 |
| 11 | Known limitations and risks | 未解决的失效模式、可移植性保留意见、安全顾虑、红队发现 | 推荐 |

为什么必须有它：本体系的多个机制都以「harness 是什么」为输入——G×S 二维定级需要执行拓扑与动作基底（字段 4、5）；E 子系统的采集点选择需要可观测性说明（字段 8）；「谁有权修改规则」一节需要控制工件与运行时策略的清单（字段 2、3）。缺了这份披露，前述条款只能依赖问询与信任，无法依赖核查。
⚠️ **定位限定**：来源为 C 级（立场论文，作者自陈无受控消融）。因此本卡定位为**披露要求，不是技术门槛**——它不设合格线，只要求说清楚；是否合格由定级流程判定，不由本卡判定。**不得**依据某系统未提交披露卡而直接推定其不安全。
## 一句话收敛
收敛：治理的重心仍在 L1，但证据的重心必须下移到内核——因为当 700 个 Agent 能自己长出指挥体系、并能伪造沙箱的工具调用记录时，「谁在记录」比「记录了什么」重要得多。
衡量这套体系成败的指标，不是「抓到多少次违规」，而是「人类的可委托边界扩展了多少」——有多少此前不敢交给 AI 的任务，现在敢了。
A8（规划—执行分离，候选架构）：将「二号主导任务拆解、一号沦为纯执行工具」作为候选架构。该架构在理论上更彻底——执行走样的空间被压缩到零，因为一号不持有对任务的解读权，「对策」无从产生；但它同时剥夺一号的探索能力，与 G0 / G1 级任务的效率目标冲突，且与蜂群治理存在张力：规划权集中于二号，会天然放大 7.2 的「指挥集中度」信号，导致合法系统误触发分片隔离。决定：作为候选架构，不在当前版本落地；若未来落地，须先重新标定指挥集中度阈值，或将「规划权集中」显式排除在涌现检测信号之外。
## 回灌检查清单
此类缺陷的两种形态：「消失」——条目被静默删除、约束被静默绕过；「悬空」——条目登记了，但没有回灌到执行条款。二者共同成因是：修订时只改了落点，没有回头接线。为杜绝此类缺陷复发，固化一道发布前必走的回灌检查，四项全部可脚本化（grep 关键词 + 人工确认），未完成不得发布。
① 相邻段落同步：新增或修订的条款，是否已同步到它上方紧邻的原则段与下方紧邻的执行段？——经验教训：8.4 新增分级处置链后，未回头修订上方原则段「任何不一致本身就是最高优先级事件」，导致同节自相矛盾。
② 相关表格同步：术语与定性的变更，是否已同步到所有引用它的表格？——经验教训：2.4 已将 E1 改为「不构成最终信任根」，但 8.5 表格仍写「信任根」，未同步。
③ 进度表与成熟度同步：正文新增的安全阀或约束，是否已同步到落地路线与成熟度判断？——经验教训：7.3 规定「阈值标定完成前 S3 不得启用硬熔断」，若落地路线仍列为「S3 级强制」交付，按路线执行即违反正文约束。
④ 变更日志与未解清单同步：本次修订是否全部登记进 CHANGELOG.md 与未解问题清单？——经验教训：C12 / C13 / C14 / REFERENCES 曾在早期版本中静默消失且未登记，后续已登记为 #9–#14。
上述四项是本体系全部缺陷的共同成因。它们不是内容问题，而是流程问题：内容质量的提高（更诚实地登记未解）并不会自动带来接线质量的提高。因此本清单与 CHANGELOG.md 并列，作为后续每一版修订的强制发布门槛。
