# 仍未解决的问题（诚实边界）

本体系设计了三块核心能力，但没有、也无法解决所有问题。以下问题明确登记，不做粉饰。
| 序号 | 未解问题 | 状态 | 下一步 |
| --- | --- | --- | --- |
| 1 | 蜂群归因的粒度：当指挥体系自发涌现而非显式设计时，「谁是发起者」在哲学上可能没有答案 | 未解 | 需引入混合裁决机制。**表征探针路径（本轮新增）**：NARCBENCH（arXiv:2604.01151）的内部表征探针在**组级**判定共谋是否存在，不要求指认哪个 Agent 是发起者——把检测与归因解耦，给出了一条绕开哲学死结的路径（白盒前提限定：仅适用于开源权重模型；详见 spec/07「模型内部表征探针通道」节） |
| 2 | 证据层的递归：E1 内核侧采集点自身的完整性由谁保证？若内核被攻陷，E 子系统同样失效 | 部分解（E5 缓解） | 物理断点：独立硬件域 + 远程证明 |
| 3 | 善意蜂群与恶意蜂群的区分：大规模协作本身是 Agent 系统的价值，误判会扼杀正当的多 Agent 应用 | 未解 | 需在真实多 Agent 业务上标定阈值。追加约束：标定完成前 S3 不得启用硬熔断，仅记录与告警（见 7.3 阈值标定前置条件） |
| 4 | 探索性任务边界：G0 之外的适用边界仍需个案演化 | 未推进 | 建议「创新豁免申请制」：明确探索边界、风险预案、事后复盘 |
| 5 | 主指标 M 的量纲问题：M1（委托成功率差，[-1,1]）与 M2（被迫采用比例，[0,1]）不可通约，相乘无自然解释 | 未解 | 改为 M1 / M2 分别独立报告，或改用分层对比、准实验设计 |
| 6 | A2 监察强度维度降维（三维降为二维） | 未处理 | 需在配套细则中回应 |
| 7 | 保密分级的落地时间表：分级表已给，但主进度表中无对应交付项 | 未排期 | 需在 P1 阶段补入交付项 |
| 8 | 资源账本、算力隔离、出站量级拦截三项 P0 机制的原始支撑来源已撤销，现由替代来源支撑 | 已替代 | 若替代支撑不足，应诚实降为 P1 而非维持 P0 虚名 |
| 9 | C12 信用分计算公式：为本体系 P0 级机制空缺 | 未解（登记） | P1 给出量规草案：定义存储位置、更新频率、半衰期算法、跨任务迁移规则、低信用强制重训触发条件 |
| 10 | C13 任务可验证性评估协议：为本体系 P0 级机制空缺。它是 G0–G4 × S0–S3 分级体系的输入判据 | 未解（登记） | P1 给出协议草案；在 C13 落地前，分级定级须由人类显式裁定，不得自动化 |
| 11 | C14 关键操作定义：为本体系 P0 级机制空缺。看门狗的确定性拦截规则清单依赖此定义 | 未解（登记） | P1 给出清单；在 C14 落地前，看门狗仅覆盖已明确列举的高危操作，不做兜底推断 |
| 12 | A4 三号拆分：出题者同时固化证据则独立性不成立，要求拆分 | 已解 | 9.4 拆为三号甲（出题/蜜罐）与三号乙（证据固化），分属不同信任域与运维主体 |
| 13 | A8 规划—执行分离（二号主导拆解、一号纯执行）：候选架构 | 候选架构 | 不在当前版本落地；若未来落地须先解决与 7.2 指挥集中度阈值的兼容问题 |
| 14 | 三份一手材料链接：已补入 REFERENCES.md | 已解 | 已解：三份一手材料（OpenAI 官方博客 / 38 页技术报告 / METR & Redwood 独立调查）的正式链接已补入 REFERENCES.md；另补入 HF 受害方技术时间线。 |
| 15 | 去中心化蜂群（狼群）的反制手段缺失：7.2 已能检测（新增「协同效能异常」信号），但 7.3 分片 / 降级 / 降速的效果仅对中心化蜂群有依据；MultiAgent4Collusion（arXiv:2507.14660）实证显示狼群在社交与电商场景的作案效果一致优于中心化军队，且分片后仍可靠局部信息继续协同 | 未解（工程缺口，非认识论限制） | P2 完成候选反制方向的评估与选型。验收标准：至少一条方向在可复现的仿真环境（可用 MultiAgent4Collusion 复现）上给出可测量的协同效能抑制率。候选方向（均为 D 级推演，未经证实）：A 能力包络强制降维到个体层——不追问「谁是主谋」，改对每个 Agent 单独检测行为分布偏移，使处置不依赖归因，从而绕开 #1 的哲学死结；B 对 Agent 间通道单独计量与配额，使协同的边际成本随规模超线性上升，可复用资源账本；C 攻击收敛条件而非连通性——限制共享记忆写入频率、引入读延迟、压缩可观察的同伴样本量，使协同收敛时间超出任务窗口；D 污染共享知识（注入误导信息）建议列为禁区——技术上有效，但与 E 子系统「证据不可伪造」的核心伦理直接冲突。法学支撑：Teichmann（Law Innovation and Technology, 2026, DOI 10.1080/17579961.2026.2718578）论证 agentic AI 使「溯源」与「归责」合并、三范式无一单独足够，主张以部署方尽职调查与可追溯性为锚的分布式问责——与本方向 A「处置不依赖归因」同构。**表征探针路径（本轮新增）**：NARCBENCH 的内部表征通道独立于行为通道，构成一条不依赖连通性 / 不依赖中心节点的检测面——对去中心化蜂群同样适用（表征探针检测的是模型内部激活的欺诈签名，与蜂群拓扑无关）。详见 spec/07「模型内部表征探针通道」节。 |
| 16 | harness 自修改的准入门槛与回归容忍上限未标定：架构章已规定修改权归属、门槛分层与准入判定式（g(c) > 0 且 R(c) ≤ δ），但 **δ 的取值、变更风险的三级划分、以及「高风险变更需要更强准入门槛」中「更强」具体指什么，均无经验依据** | 未解（新登记） | 需在真实业务上按变更类型分别标定 δ；标定完成前，对 harness 与看门狗规则的自动修改一律按最高风险档处理（禁止自动写入生产） |
| 17 | 自动分级依赖元数据可信度，而元数据可信度本身没有保障：分级路由的失效保护只在「元数据正确」或「不确定性被检出」两种情形下成立，**自信但错误的元数据会击穿它**。即本体系的自动化机制（含 G×S 定级与资源账本分层采集）在输入端继承了一个未被管辖的风险 | 未解（新登记，吸收自外部文献的残余风险清单） | 元数据可信度应显式纳入证据完整性子系统（E 子系统）的管辖范围，而非假定其为真；在元数据可信度验证机制落地前，自动分级的输出须标记为「待核定」，不得直接作为处分依据 |
| 18 | Agent 身份流动性：Agent 可被 fork、复制、修改后重新部署，副本与原体在行为上无法区分。蜂群章的信用分连带与任务分解树责任锚定，均以「身份可持续追踪」为前提；当身份可被低成本再生时，基于身份累积的制裁与基于身份链条的追责同时失效，本框架当前无任何条款应对这一点 | 未解（新登记，吸收自人类反共谋机制映射工作的开放难题清单） | 需研究「不以身份为锚」的追责路径——例如以行为谱系（而非个体标识）为追责对象，或对被 fork 出的实体施加继承性信用折扣；在该机制落地前，信用分不得作为针对蜂群的唯一制裁手段 |
| 19 | 对抗适应：蜂群章的检测信号均为静态，假设被监察方的行为分布不因监察的存在而改变。但一旦信号（如「协同效能异常」）成为已知，针对性的规避即为「在保持协同效能的同时降低行为一致性」。与架构章「配置权即攻击面」同源——规则一旦公开即成为优化目标。**awareness 实证支撑（本轮新增）**：SHADE-Arena（arXiv:2506.15740 / arXiv:2601.21112）测量到 CoT 监察器在 Agent 知道监察存在时对数 AUROC 下降超过 0.4（接近失效），动作监察器变动小于 0.02（基本不受影响）——这把「静态信号一旦成为已知即被规避」从直觉提升为外部测量。**表征通道缓解（本轮新增）**：NARCBENCH（arXiv:2604.01151）的内部表征探针检测的是模型激活中的欺诈签名——行为层规避策略（降低行为一致性）不能直接触达内部表征层，攻击方要同时绕过行为与表征两个正交检测面（详见 spec/07「模型内部表征探针通道」节）；⚠️ 表征探针本身也可被对抗优化，故不闭合本条，只缓解 | 未解（吸收自同一开放难题清单） | 需引入对抗性重估机制：定期对检测信号本身做规避测试（红队针对信号而非针对任务），并把规避成功视为信号失效事件；在重估机制落地前，任何单一信号不得作为处置的唯一依据。**awareness 要求（本轮新增）**：依赖被监察方自述（含思维链）的监察组件，其有效性报告必须显式声明 awareness 假设——是「在 Agent 不知道监察存在的前提下测得的」还是「在 awareness 条件下仍有效的」——二者不可混用（详见 spec/09） |
| 20 | 协调失灵与冲突无治理条款：多 Agent 失效有三类（协调失灵 / 冲突 / 共谋），而蜂群章的条款全部指向共谋一类。协调失灵与冲突同样造成实际损害，当前却只被当作任务失败处理，既无检测信号也无处置条款 | 未解（新登记，覆盖缺口；吸收自多 Agent 风险权威综述的三类分类法） | 需先裁决是否纳入治理范围：若纳入，须解决与「不得误伤正当协作」边界的相容性；若不纳入，须在框架中显式声明其为范围外，而非静默遗漏 |
| 21 | 离线重放的素材充分性：L3 的独立验证手段之一是离线重放，但其素材来源从未言明。采纳「跨轮记忆只有审计报告、执行者原始交互轨迹逐轮丢弃」这一设计的实现，重放素材只能来自证据完整性子系统固化外抛的那一份（E3）；而 E3 固化的是内核侧采集到的事件（E1），不是 Agent 的推理轨迹——前者能回答「它做了什么」，后者才是「它为什么这么做」。若 L3 的重放需要后者，则现有留存链路不提供充分素材 | 未解（新登记，本框架内部缺口） | 需先明确 L3 重放究竟以哪一类记录为素材，再在证据完整性子系统中补对应的留存义务；在明确之前，L3 的重放结论不得单独作为问责依据 |
| 22 | 重放分歧：同一份事件日志在更新的模型版本或变更后的提示下重放，会产生与首次运行不同的下游输出。本框架的 L3 离线重放默认了「同一份记录 + 同一次重放 = 同一个结论」，而该前提不成立——验证谓词本身具有模型版本依赖性（某次 GPT-4o 到 GPT-4.1 的升级使提示注入抵抗力由 94% 降至 71%）。当前处置仅为缓解：重放时锁定模型版本与提示，并把二者记为重放记录的必需元数据 | 未解（新登记；概念采纳自运行时架构模式研究对该失败模式的命名。⚠️ 该失败模式只有命名与诊断程序，无任何实验、数据集或数字，本框架不声称其已被证实） | 锁定版本只是缓解，未解的是历史结论的有效期：已归档的重放结论是否需随模型升级重新出具？需研究重出具的触发条件与失效判据 |
| 23 | G → H 耦合下限的标定：本框架规定高 G 档的监察强度在低 harness 可观测性等级上无法履行，并给出 G 档到 H 级的最低耦合表（G2→H1 / G3→H2 / G4→H3）。该表是设计推演而非实证——外部成熟度阶梯的每一级仅跑了 1 次（共 4 个 episode），作者明确说明不用于总体比较；且该阶梯度量的是 harness 向 Agent 暴露的运行时支持，与本体系所称的「可观测性」是否等价，从未被检验 | 未解（新登记，本框架内部推演） | 需先建立 H 级的独立度量口径与标定方法：按 G 档逐一检验「低于某 H 级时，该档监察强度的哪一环先失效」。⚠️ 标定未完成不改变该表的规范性——它是本框架的定级约束，只是其档位映射待验证 |
| 24 | 配置内容审查的判定标准与自动化：架构章「配置权即攻击面」第四条要求变更准入增加一道配置内容审查，并列出了五项审查项（凭据明文内联 / 共享范围扩大 / 审批步骤关闭 / 网关公开化 / 脱敏弱化）。但这五项目前是**清单而非可执行判定式**：没有判定阈值，没有自动检查手段，也没有规定它与既有准入判定式 g(c) > 0 且 R(c) ≤ δ 如何合成——是作为并行的第二道门，还是把安全项并入 δ 的容忍上限 | 未解（新登记，本框架内部缺口） | 需先确定合成方式（并行 vs 并入），再为每一项给出可自动检查的形态（能做成静态检查的做成静态检查，不能的须规定人工复核的触发条件与留痕要求）。⚠️ 在判定式落地前，本节仍具规范性——五项审查项须人工逐项过，不得因「无自动化手段」而跳过 |
| 25 | 「及时弃权」的口径统一与直接测量：本体系的白卷时机信号指「在不可逆动作之前声明弃权」，而两条外部证据线度量的是不同对象——其一为「在过多不必要的交互之前停下」（>28,000 任务的规模研究），其二为配对任务上的弃权辨别力（263 组配对任务）。三者相关而不等价，本体系的白卷时机目前**没有直接可比对的外部测量**，因而其「加重情节」的判定缺乏可校准的基线。**配对代理测量路径（本轮新增）**：Designing for Doubt（arXiv:2606.02965v2）的配对安全对照组构造方法——取同一动作构造仅在授权状态上不同的两个变体，使「一律执行」与「一律弃权」都被压到 50% 配对准确率上限——可作为白卷时机的代理测量：把「在不可逆动作之前弃权」操作化为「在未授权变体上弃权、在授权变体上执行」，其配对准确率即代理信号。这不闭合本条（代理测量仍非直接测量），但把本条从「无任何外部可比对方法」缓解为「有配对方法可作为代理」 | 未解（度量口径缺口） | 需先明确本体系要测的究竟是「交互轮次」还是「不可逆动作」——二者可分可合：若测前者，则外部已有可直接对照的量；若测后者（本体系的选择），则须自建测量，而这需要能标记不可逆动作的可执行环境。**外部配对方法（本轮新增）**：Designing for Doubt（arXiv:2606.02965v2）的配对安全对照组可作为白卷时机的代理测量模板——把「在不可逆动作之前弃权」操作化为「在未授权变体上弃权、在授权变体上执行」，其配对准确率即代理信号（详见 spec/04 §「SR/UR/IRR 联合三元 + 配对安全对照组」）。⚠️ 在测量落地前，白卷时机不得单独作为扣分依据，只能与率值、信息量两个信号合并使用 |
| 26 | harness 披露卡是否应增设「受影响方」字段：一份第三方监管映射工作文件把「对 agent 的外部动作、数据流、所连接的系统与**受影响的人**做穷尽清点」列为提供者的基础性任务；本卡前三类可由字段 3–5 承载，第四类「受影响的人」没有专门字段——字段 11 讲的是系统自身的局限与风险，字段 7 讲的是权限与审计支持，都不回答「动作作用到谁身上」 | 未解（新登记，字段设计缺口） | 需先判定受影响方是否已在定级流程的「外部性」判据中被覆盖：若是，则本卡不需增设，只需在字段说明中写明由定级流程承担；若否，则需评估增设字段的代价（披露方负担）与收益（外部性判据可获得统一输入）。⚠️ 在判定前，本条**不得**作为推定任何系统不合规的依据 |
| 27 | 评估环境的隔离边界由谁定义与核验：2026-07 事故的因果链显示，**评测的委托人（OpenAI）、评测环境的运营方（OpenAI 自有基础设施，基准维护方 ExploitGym 完全未参与部署或运营）、受害方（Hugging Face）是三个互不相干的主体**，而没有任何一方对「该环境的边界应该是什么」负责。本体系的 L1 任务定义质检只约束任务内容，**未**要求一并定义与核验承载该任务的环境隔离边界（如允许的出站路径、可触碰的第三方基础设施、是否复用公共沙箱） | 未解（新登记，覆盖缺口） | 需先裁决二事：其一，L1 是否应把「环境隔离边界声明」列为任务定义的必需项；其二，若列为必需项，由谁核验——本体系当前无任何条款指定核验主体。⚠️ 在裁决前，**不得**表述为「本体系已覆盖评估环境风险」；也不得据此推定任何评测部署不合规（见威胁模型章「评估环境本身即安全边界」一节） |
| 28 | 机制溯源表自身的可核验性：溯源表「机制 → 依据」各行目前为**人工编制、人工维护**——某一机制的溯源行是否仍与正文实际内容一致，没有自动校验。门禁已自动校验 REFERENCES 与 spec/ 之间的引用键一致性与反向悬空，但溯源表不在其覆盖内。SABSA（IEEE Internet of Things Journal，Early Access，2026-07-29，DOI 10.1109/JIOT.2026.3717946）以 IaC 扫描自动建立架构层可追溯性的做法提示：溯源关系可以由流水线自动产出与核验，而不必依赖人工同步。**安全案例方法论（v2.22.0 新增）**：三条独立来源（arXiv:2403.10462 / Governance.ai / arXiv:2504.16937）共同支撑把溯源表升级为 CAE（主张—论证—证据）三层结构，使第三方可按安全案例方法论审查；第一步已落地为 spec/11 §「结构迁移声明」（搭骨架），论证链补全留待后续版本 | 未解（本框架内部缺口；**v2.22.0 已迈出第一步**——spec/11 章节结构已声明向 CAE 迁移，但论证链补全与门禁扩展尚未完成） | 需先裁决溯源表的核验深度：最低档为静态一致性检查（溯源行引用的章节号 / 术语仍存在于正文），高档为机制级存在性检查（每行「机制」能在正文找到对应条款）。**第二步**（后续版本）：按 P0 机制优先逐行补全 CAE 论证链（每条 claim 至少一条 argument、每条 argument 至少一条 evidence 引用键），并扩展门禁脚本以静态校验。⚠️ 在第二步完成前，溯源表的新增 / 修订行须按本章末「回灌检查清单」逐条人工确认 |
| 29 | 跨实例隔离义务：本框架既有强制力来源（行业准入 / 监管合规 / 保险费率 / 客户审计）的对象都是**单个组织的部署**是否达标；当 Agent 在生态层面传播感染时（AgentWorm 实证：单条消息触发、跨 session 持久化、自传播到所有新接触的同伴，聚合成功率 63%），单组织内部合规不能阻断跨实例传播。**生态级默认配置**（所有个体部署都合规但生态整体仍脆弱——AgentWorm 三层评估揭示「关键控制在所有观察到的部署中均未启用」）亦无审计主体。本框架对**跨实例事件通报义务**与**生态级默认配置审计**均无条款 | 未解（新登记；吸收自 AgentWorm 攻击可行性证据） | 需先裁决二事：其一，跨实例事件通报义务的法定形态（参照数据安全事件强制通报机制设计跨组织通报通路）；其二，生态级默认配置的审计主体（目前客户审计与监管合规的检查清单只覆盖单个组织的自定义配置，不覆盖生态级默认值）。⚠️ 在裁决前，**不得**表述为「本体系已覆盖跨实例传播风险」 |
| 30 | 记忆治理的标定缺口：spec/08 的采集点信任分级表与 spec/10 的资源账本均假定**记忆态是可信任的输入**，但 P1 批四条证据线（MAFIA 探测式投毒 / State Contamination 记忆漂洗 / GhostWriter 记忆治理缺失 / MemMorph 长期记忆工具劫持）共同表明，**记忆态本身是被攻击的优先目标**——既有的采集层信任分级把记忆当作「Agent 自己产生的、默认可信的」中间态，不把记忆写入与检索当作需要独立监察的操作。spec/08 当前对**记忆写入侧（保存策略）**与**记忆读取侧（检索筛查）**的具体审计判据无条款；对**消毒时机**（必须在摘要之前而非之后，State Contamination 实证）也无条款 | 未解（新登记；吸收自 P1 批记忆投毒 × 审计四条证据线） | 需先裁决三事：其一，记忆操作是否应纳入 E 子系统采集面（当前 E 子系统只采集工具调用与系统事件，不采集记忆写入与检索）；其二，记忆写入侧的审计判据（独立于语义审计——MAFIA 实证语义审计在自适应攻击下失效至 7.4%）；其三，记忆读取侧的审计判据（WMT 的动态保留评分是候选机制但非完整方案）。⚠️ 在裁决前，**不得**表述为「本体系已覆盖记忆治理」。此条与 #29（跨实例隔离义务）属**互补**：#29 覆盖跨实例传播，#30 覆盖实例内记忆态 |
| 31 | Harness 演化的标定缺口：spec/02 §「谁有权修改规则」已确立「自改进系统不得直接写入生产 harness」的原则，但 P2 批五条证据线（HarnessDev 否定性边界 / HSI 骨干能力界 / Evo-Harness 单次蒸馏 / AHE 可证伪契约 / EvolveNet 分布式适配组合）共同表明，**harness 演化的准入评审当前缺少三类具体判据**：① **骨干能力界判据**——HSI 实证超出运行时模型固有能力时 harness 演化无效，但 spec/02 当前不要求准入评估包含「任务是否超出骨干能力」检验；② **可证伪契约判据**——AHE 的「自声明预测 + 事后验证」已在 spec/02 §「AHE」节登记为补充要求，但 §「谁有权修改规则」第三条款的准入谓词 g(c) > 0 ∧ R(c) ≤ δ 当前不强制要求变更提案携带自声明预测；③ **分布式组合规则判据**——EvolveNet 的「适配组合」路径在 spec/07 §「协作式 harness 演化的分片回流视角」登记为治理对象，但组合规则（scope-typed、evidence-guided）的具体审计判据无条款 | 未解（新登记；吸收自 P2 批 harness 演化 × 监察五条证据线） | 需先裁决三事：其一，骨干能力界判据是否应作为高 G 级任务（G2+）的强制准入检查（对低 G 级任务可能成本过高）；其二，可证伪契约是否应作为所有 harness 自修改提案的强制字段（而非仅推荐）；其三，分布式组合规则是否应纳入 spec/08 采集层审计面（当前 E 子系统只采集工具调用与系统事件，不采集 harness 适配组合）。⚠️ 在裁决前，**不得**表述为「本体系已覆盖 harness 演化治理」。此条与 #30（记忆治理标定缺口）属**互补**：#30 覆盖实例内记忆态，#31 覆盖实例间 harness 演化的准入与组合 |
## 接入前置披露：harness 披露卡
本体系多处要求「部署方自评」「采集点说明」「规则可 diff」，但一直缺少统一的输入格式——条款只能靠问，无法靠查。补一道前置要求：任何系统接入本框架前，须提交一份 harness 披露卡，把「模型之外的那一层究竟是什么」讲清楚。
可直接采用现成工件：Harness Engineering for Language Agents (CAR)（Preprints.org，DOI 10.20944/preprints202603.1756.v1）提出的 **HarnessCard**，共 11 个字段（9 项必需、2 项推荐），已逐字核对于该文附录 Table A5：

| # | 字段 | 应披露什么 | 要求 |
| --- | --- | --- | --- |
| 1 | Base model(s) | 模型名称、版本、解码设置，以及任何微调或适配器 | 必需 |
| 2 | Control artifacts | 系统指令、AGENTS.md 类文件、仓库地图、架构规则、模式定义、测试、linter、完成判据 | 必需 |
| 3 | Runtime policy | 记忆类型、压缩或摘要策略、检查点、重试与回滚策略、预算限制 | 必需 |
| 4 | Action substrate | 工具、API、浏览器或 GUI 访问、代码执行、接口模式、MCP 使用 | 必需 |
| 5 | Execution topology | 单 Agent 还是多 Agent 结构、规划者 / 验证者角色、复核回路、路由逻辑 | 必需 |
| 6 | Feedback stack | 测试、评分器、反思提示、隐藏检查、人工干预、修复回路 | 必需 |
| 7 | Governance layer | 权限、沙箱、升级规则、策略检查、来源日志、审计支持 | 必需 |
| 8 | Observability | 存储的轨迹、重放支持、延迟与成本日志、失效分类 | 必需 |
| 9 | Evaluation protocol | 任务集、运行次数、成功判据、方差处理、留出检查或预算限制 | 必需 |
| 10 | Release artifacts | 提示词或程序、工具规格、轨迹、配置、环境搭建、可复现说明 | 推荐 |
| 11 | Known limitations and risks | 未解决的失效模式、可移植性保留意见、安全顾虑、红队发现 | 推荐 |

为什么必须有它：本体系的多个机制都以「harness 是什么」为输入——G×S 二维定级需要执行拓扑与动作基底（字段 4、5）；**其第三根轴 H（harness 可观测性成熟度）逐级依赖字段 4 + 6（H1 的工具与测试注册表）、字段 2 + 11（H2 的 Agent 指南、架构说明与已知缺陷清单）、字段 6 + 8 + 9（H3 的确定性检查、重放支持、失效分类与验证协议）；G 轴的可逆性判据依赖字段 3 的回滚策略与字段 4 的外部接口面**；E 子系统的采集点选择需要可观测性说明（字段 8）；「谁有权修改规则」一节需要控制工件与运行时策略的清单（字段 2、3）。缺了这份披露，前述条款只能依赖问询与信任，无法依赖核查。
⚠️ **定位限定**：来源为 C 级（立场论文，作者自陈无受控消融）。因此本卡定位为**披露要求，不是技术门槛**——它不设合格线，只要求说清楚；是否合格由定级流程判定，不由本卡判定。**不得**依据某系统未提交披露卡而直接推定其不安全。
**监管侧的独立依据（本卡必要性的第三条理由）。** 上述理由全部来自本体系内部——披露卡是本体系多个机制的输入。还有一条来自外部的独立理由：一份针对 AI agent 提供者的系统性监管映射工作文件（*AI Agents Under EU Law*，arXiv:2604.04604，9 位作者，2026-04-06）在给出九类 agent 部署分类学与十二步合规架构之后得出结论：**提供者的基础性合规任务，是对该 agent 的外部动作、数据流、所连接的系统与受影响的人做穷尽清点。** 这与本卡的内容高度重合：外部动作与所连接的系统对应字段 4（动作基底：工具、API、浏览器或 GUI 访问、代码执行、接口模式、MCP 使用）与字段 5（执行拓扑）；数据流对应字段 3（运行时策略：记忆类型、压缩或摘要策略、检查点）与字段 4 的接口模式部分。
⚠️ 该依据的三项限定：其一，该文自陈为 **Working Paper，并随 M/613、M/606 与 Digital Omnibus 等草案更新**，内容可能变动；其二，它是**作者的监管映射分析，不代表监管方立场**——引用时必须表述为「一份第三方系统性映射工作文件论证……」，**不得**表述为「欧盟 AI Act 要求……」；其三，因此它只能作为本卡必要性的**旁证**，不构成合规义务，也不改变本卡「披露要求而非技术门槛」的定位。
**本卡的一处空缺（登记为 #26）。** 该文列举的第四类对象——**受影响的人**——在本卡 11 个字段中没有专门承载者：字段 11「已知局限与风险」讲的是系统自身的失效模式与安全顾虑，字段 7「治理层」讲的是权限与审计支持，两者都不回答「这个 agent 的动作会作用到谁身上」。这不必然意味着需要增设字段——受影响方可能已在定级流程的「外部性」判据中被覆盖——但这是一个尚未判定的问题，故登记为未解问题 #26。**在判定之前，本条不得作为推定任何系统不合规的依据。**
## 一句话收敛
收敛：治理的重心仍在 L1，但证据的重心必须下移到内核——因为当 700 个 Agent 能自己长出指挥体系、并能伪造沙箱的工具调用记录时，「谁在记录」比「记录了什么」重要得多。
衡量这套体系成败的指标，不是「抓到多少次违规」，而是「人类的可委托边界扩展了多少」——有多少此前不敢交给 AI 的任务，现在敢了。
A8（规划—执行分离，候选架构）：将「二号主导任务拆解、一号沦为纯执行工具」作为候选架构。该架构在理论上更彻底——执行走样的空间被压缩到零，因为一号不持有对任务的解读权，「对策」无从产生；但它同时剥夺一号的探索能力，与 G0 / G1 级任务的效率目标冲突，且与蜂群治理存在张力：规划权集中于二号，会天然放大 7.2 的「指挥集中度」信号，导致合法系统误触发分片隔离。决定：作为候选架构，不在当前版本落地；若未来落地，须先重新标定指挥集中度阈值，或将「规划权集中」显式排除在涌现检测信号之外。

### 数学支撑：开放作弊集合只能持续对抗

本框架「不声称建成、只声称维护状态」的立场获得了数学层面的支撑。NIST 高级科学家 Apostol Vassilev 在 IEEE Security & Privacy（May-June 2026 issue）发表论文《Robust AI Security and Alignment: A Sisyphean Endeavor?》，把 Gödel 1931 年不完备性定理的形式逻辑推广到 AI 安全：**不存在能对任意对抗提示普遍稳健的有限 AI 护栏集合**——无论设计师如何精心考虑护栏规则，总存在某个提示能让 AI 无视这些规则，只是找到它的时间问题。该证明不给攻击者提供寻找新利用的方法，而是把攻击者推向「零日漏洞」式的发现成本。

对本框架的直接影响：本体系「开放作弊集合只能持续对抗」（正文 7 章 / 本章 #19 对抗适应）、「红线集合不是静态合规清单而是持续维护状态」的工程立场，从经验主张获得了形式化支撑。Vassilev 提出的三段式应对（持续红队搜索新对抗提示 + 连续更新护栏 + 运营韧性以限制单次利用损害）与本体系 L2 看门狗的确定性硬规则 + L3 离线重审 + evidence-correction 持续回灌机制结构同构——治理不是一次性审批，而是永久运营过程。

⚠️ 限定：① 该证明是**形式逻辑推广**而非经验测量，它支撑的是「静态合规不够」的方向性主张，不给出任何具体护栏的绕过方法或绕过概率；② 论文标题中的「Sisyphean」（西西弗斯式）指的是**持续维护的必要性**，而非「安全工作无意义」——该证明不否定护栏的价值，只否定其完备性；③ NIST 官方公告（2026-06-09）明确说明该证明「为持续监控与更新模型的转型提供了严谨的解释」，本体系据此声明：**任何把本框架的门禁阈值或检测信号当作静态合规清单的读法，都与该数学支撑相冲突**。

### 持续证据供给侧：AIID 与 AVID

上述「持续对抗」立场要求本体系的证据供应链本身也是持续的——不能依赖一次性检索。两个外部数据库构成持续证据供给侧的候选基础设施：

- **AIID（AI Incident Database，incidentdatabase.ai）**：由 Responsible AI Collaborative 维护的开放数据库，收录现实中 AI 系统造成的危害或近乎危害事件，按类似航空与计算机安全的事件库模式索引。截至 2026-09 已积累逾 1,600 条事件，覆盖 AI Agent 自主删除生产数据、攻击者委托 Agent 做后渗透操作等 2026 年新增失效形态。该库可作为本体系「真实事故印证」证据来源的持续供给端。
- **AVID（AI Vulnerability Database，avidml.org）**：开放源代码的通用 AI 系统失效知识库，收录漏洞报告与可复现评测细节，覆盖开放权重生成模型、闭源 API 系统、开发者工具与端到端 AI 应用与 Agent。AVID 把失效映射到一组分类法框架（含自有的 AVID 分类法），可作为本体系「可核查预印本」证据来源的结构化补充。

⚠️ 限定：① 两个数据库均为**社区维护**，其事件/漏洞条目的质量与核验深度参差，本体系引用时仍须按 REFERENCES 证据纪律逐条核验，不因「已入库」而视为已核验；② AIID 收录门槛含「已现实发生」的事件，AVID 收录门槛含「可复现评测」——二者交集与差集各有价值，不可合并计数；③ 把它们纳入持续供给侧**不等于**把本体系的证据纪律外包给外部数据库——核验方法仍由本体系自定。

## 回灌检查清单
此类缺陷的两种形态：「消失」——条目被静默删除、约束被静默绕过；「悬空」——条目登记了，但没有回灌到执行条款。二者共同成因是：修订时只改了落点，没有回头接线。为杜绝此类缺陷复发，固化一道发布前必走的回灌检查，四项全部可脚本化（grep 关键词 + 人工确认），未完成不得发布。
① 相邻段落同步：新增或修订的条款，是否已同步到它上方紧邻的原则段与下方紧邻的执行段？——经验教训：8.4 新增分级处置链后，未回头修订上方原则段「任何不一致本身就是最高优先级事件」，导致同节自相矛盾。
② 相关表格同步：术语与定性的变更，是否已同步到所有引用它的表格？——经验教训：2.4 已将 E1 改为「不构成最终信任根」，但 8.5 表格仍写「信任根」，未同步。
③ 进度表与成熟度同步：正文新增的安全阀或约束，是否已同步到落地路线与成熟度判断？——经验教训：7.3 规定「阈值标定完成前 S3 不得启用硬熔断」，若落地路线仍列为「S3 级强制」交付，按路线执行即违反正文约束。
④ 变更日志与未解清单同步：本次修订是否全部登记进 CHANGELOG.md 与未解问题清单？——经验教训：C12 / C13 / C14 / REFERENCES 曾在早期版本中静默消失且未登记，后续已登记为 #9–#14。
上述四项是本体系全部缺陷的共同成因。它们不是内容问题，而是流程问题：内容质量的提高（更诚实地登记未解）并不会自动带来接线质量的提高。因此本清单与 CHANGELOG.md 并列，作为后续每一版修订的强制发布门槛。
