# ── 内部工作文件（禁止入库）──────────────────────────
.workbuddy/
.workbuddy_commit_msg.txt
.commit_msg_*.txt

# ── 备份与临时文件 ──────────────────────────────────
*.backup*
.__tmp_*

# ── 系统文件 ────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── 注：开源前准备方案 / B1 清单等内部工作文档 ────────
# 如误放入本目录，会被推上 GitHub；请确保不入库
