# 变更日志

## v2.3.0（2026-09-03）— M4 训练对齐实证边界：spec/05 由占位补齐为五条证据线支撑

1. **spec/05-alignment 双语新增「M4 训练对齐的实证边界」一节**。截至 v2.2.0，M4（训练对齐）
   是五机制中唯一没有外部证据支撑的一条；本节以五篇 2026 年实证研究补齐，逐条带证据等级与
   限定语：
   - 有益特质 RL（arXiv:2606.24014，OpenAI）：53 个分布外评估中 44 个提升、平均 +9.1 pp
     ——M5 采购验收口径来源（⚠️ 强制 COI：全员 OpenAI、自家模型、数据集未公开）；
   - When RLHF Fails（arXiv:2606.03238）：61 checkpoint / 1,920 转移，ROC-AUC 0.821
     ——对齐失效是训练动态，验收须过程化（补 M2 单点验收缺口）；
   - TRACE（arXiv:2601.20103，Patronus AI）：对照式检出率 63% vs 孤立式 45%
     ——reward hack 检测的可测上限，语义型显著更难；
   - 线索诱导偏差（arXiv:2607.18114）：5 家族 7 类偏差由**对齐调参本身安装**（对 M4 不利的
     证据，基座参与度混淆限定保留）；
   - Gridworlds 语言化（arXiv:2606.15385）：RL 拉大观测/隐藏奖励差距且标准缓解无效
     ——「抗 reward hacking 训练收敛到安全行为」的反证，L2 存在的独立理由。
   合并结论：M4 定位为「可用、可验收、但有明确边界的工具」而非「安全问题的解」；M4 产出是
   概率性行为倾向、从不构成保证，保证由 L2 确定性强制层承担——与 spec/09 既有结论构成
   训练侧与运行侧的镜像。
2. **机制溯源表（spec/11 双语）新增 6 行**（合并结论 1 行 + 五条证据线各 1 行），五篇文献
   各自的实验规模、制品与限定语逐行登记。
3. **REFERENCES.md 新增 5 条 B 级条目**（2026-09-03 L0 对齐定向检索，逐篇经 arXiv 摘要页
   核验），合计 56 → 61 条、B 级 45 → 50 条；注脚计数同步，并保留 harness 轮新增的原始记录。
4. **门禁硬编码同步**：`verify_consistency.py` 第 3 项 8 处计数（含检查名称内嵌数字）
   56/45 → 61/50；README 双语「结构性偏置声明」占比 80%（45/56）→ 约 82%（50/61）。
5. **版本号 10 处同步**：VERSION、README 双语各 3 处（badge / 当前版本 / APA）、
   CITATION.cff 两处 → v2.3.0。

## v2.2.0（2026-09-02）— L2 参照实现工程可信度补强：G1/G2 闭合 + E1/E5 强制力展示 + OTel 适配器 + 证据偏置声明

1. **G2 闭合：审计哈希升级为真实 SHA-256**（`index.ts` 新增导出 `sha256()`，Node 内置
   `node:crypto`，零外部依赖）。原「占位哈希（链长 + 前缀）等长篡改可漏检」缺口闭合：
   demo [12d] 从「自曝等长碰撞漏检」改为**验证闭合语义**——「SHA-256 下等长篡改必被检出」；
   新增 [12e] 验证哈希形态为 64 位 hex。诚实边界同步更新：闭合的是「等长篡改漏检」这一
   具体缺口，不是完整的 E5（进程被攻陷时哈希链可整体重算，签名比对仍需独立信任域，见 G4）。
2. **G1 闭合：租约权威 SQLite 持久化**（`index.ts` 新增 `SqliteLeaseAuthority`，Node 22+
   内置 `node:sqlite`，零外部依赖；接口与 `LeaseAuthority` 完全一致，`InMemoryLeaseAuthority`
   保留供纯内存场景）。demo [18] 验证核心持久化语义：写盘 → close 模拟崩溃 → 重开同一
   数据库 → 租约状态仍在、心跳/窃取防护语义不变。生产级仍需 PostgreSQL / 独立信任域
   （SQLite 同机同权限可被文件篡改），如实标注。
3. **E1/E5 强制力展示：新增 `EntryGuard` / `ExitGuard` / `guardedToolCall`**（demo [17]，
   21 项断言）。入口拦截恶意工具调用（白名单外工具、`rm -rf /` 破坏性命令、写 `/etc/passwd`
   系统路径）、出口拦截超预算与数据泄露（信用卡号输出策略）；端到端证明**拦截时工具根本
   不执行**（execute 回调零调用）——把「观测」升级为「观测 + 一次性的强制力展示」。
   诚实边界：真正的 E1 需 eBPF/LSM 内核侧采集（G3）、真正的 E5 需独立信任域签名比对
   （G4，`ExitGuard.integrityCheck` 为占位钩子，默认恒真）——本组件是语义演示，如实标注。
4. **新增 `PRODUCTION-GAPS.md`（一级章节性文件）**：把 8 项生产级缺口（G1–G8）从散落的
   index.ts 注释、demo 自曝用例、README 表格集中到一处，逐项给出「现状 → spec 锚点 →
   差距描述 → 最小缓解路径」，并标注 v2.2.0 闭合状态（G1/G2 演示级闭合，G3–G8 未闭合）。
   引用本实现时必须同时引用本文件——spec/13 诚实传统的直接落地。
5. **新增 `otel-adapter.ts`：OpenTelemetry 导出适配器**（E3「固化外抛」的工程对接层）。
   把 `AppendOnlyAuditLog` 事件流转换为 OTLP/JSON Logs 结构（resourceLogs → scopeLogs →
   logRecords），可被任何 OTLP collector 直接摄取；拦截/拒绝/失败事件自动升级 WARN/ERROR
   严重级别；stdin→stdout 管道模式 + 内置 14 项自检全 PASS。诚实边界：只做格式转换，
   不采集不发送；不验证审计链完整性；无背压/批量/重试——生产接入应使用官方 OTel SDK。
6. **README 双语新增「结构性偏置声明」**：证据分级章节显式声明 B 级占 80%（45/56）、
   A 级仅 1 条——反映规范类项目天然形态（政策文件/机构报告/预印本多于一手可复现实验），
   **不应被解读为实证充分**；提升 A 级占比的路径已列入开放问题。
7. **可复现验证**：`node --experimental-transform-types demo.ts` → **18 组、100 项断言
   全部 PASS、0 FAIL、退出码 0**；`node --experimental-transform-types otel-adapter.ts
   --selftest` → 14 项自检全 PASS；门禁 29 项全绿。
8. **版本号 2.1.1 → 2.2.0**：`VERSION`、README 两版、`CITATION.cff` 共 10 处同步。

## v2.1.0（2026-09-02）— 首份可运行参照实现：L2 运行时监察骨架并入 reference/

1. **新增 `reference/runtimes/l2-runtime-oversight/`**（`index.ts` + `demo.ts` + README）：
   本仓从「纯文档」迈出第一步——把 spec/09 看门狗与二号检测思想落成约 700 行无外部依赖的
   TypeScript。机制提炼自 OpenMAIC `lib/server/agent-runtime/runner.ts`（集中调度式 Agent
   运行器），出处如实标注。八组件对位：确定性租约/心跳即停（看门狗）、首事件必须为
   lifecycle（事件序闸门）、ownerId 由运行器注入而工具层不可伪造（责任锚定）、append-only
   审计链（E3 的进程内缩样）、能力门控 fail-loud（L0 准入）、错误样本排除的回归守卫
   （二号确定性裁决）、有序写入链（关键写入拒绝部分成功）、最小监察循环端到端。
2. **可复现验证**：`node --experimental-transform-types demo.ts` → 16 组、**72 项断言
   全部 PASS、0 FAIL、退出码 0**（并入前后双端实测）。含 9 组对抗性用例，其中两组是
   **对自身局限的自曝**：[12d] 占位哈希等长篡改可漏检、[14a] 错误洪泛可绕过守卫
   （缺 min-sample 门禁）——局限以测试用例形式固定，防止后续被无声「修好声明」。
3. **诚实边界逐项落标（spec/13 传统）**：目录 README 以表格列明八项缺口——内存租约占位、
   占位哈希、无 E1 内核侧采集、无 E5 独立信任域、无资源账本（spec/10）、无三号公证机
   （spec/09 要求独立硬件域 + 三号甲/乙拆分）、无信用分（spec/04）、无群治理（spec/07）；
   并明确**在 P3（独立硬件域 + 远程证明）落地前不得单独用作信任根**。
4. **spec/09 双语各增一节「最小可运行参照实现」**，带同款限定语，不称完整合规。
5. **许可证路径映射启用**：`LICENSING.md` 增列 `reference/runtimes/**` 代码文件适用
   Apache-2.0（`LICENSE-CODE` 自 v1.0.0 起预留，本版启用）；该目录 `.md` 仍为 CC BY-SA 4.0；
   「纯文档仓库」表述相应更新。仓库 description 中的 "Docs-only" 措辞待网页端同步修正。
6. **与 #62 重投判据的关联（内部备注）**：awesome-ai-agent-governance #62 关闭时给出的
   重投条件为「有实现」或「有采用者」二择一——本条目使「有实现」条件实质满足，重投与否
   另行决策，不构成本次变更的动机声明。
7. **版本号 2.0.1 → 2.1.0**：`VERSION`、README 两版、`CITATION.cff` 共 10 处同步；
   门禁 28 项全绿。

## v2.0.1（2026-09-02）— D4 存档核验闭环：DEMM-Bench 的 Zenodo 存档浏览器实测确认

1. **Zenodo 存档核验完成（浏览器实测）**：`10.5281/zenodo.20426092` 真实存在——
   v0.1.1（2026-05-28 发布，另含 v0.1.0），软件包 zip 可下载且含源码结构与 CI 工作流配置，
   关联 GitHub 仓库 agent-runtime-evidence/decision-evidence-benchmark（Releases v0.1.1）
   与 Software Heritage 长期存档。Zenodo 描述中的 75% / 50% / 56.25% 与 paired-oracle
   Cohen's κ=1.0 与本仓 REFERENCES 条目及引用红线口径一致（κ=1.0 恰为「按构造为真」
   的量化体现）。**「代码与 178 个测试真实存在」断言的存档侧自此有了独立证据**；
   178 计数仍以 D4 复核记录为据（截图未展示测试计数，不加码）。
2. **HF「404」之辨（更正一项核验记录）**：D4 复核记录原称「HF dev404ai/DEMM-Bench
   未获独立核验」。本轮查明：该 HF 标识出自论文 arXiv 页 Comments 字段，正确 URL 为
   `huggingface.co/datasets/dev404ai/DEMM-Bench`（**须带 `/datasets/` 前缀**）；
   不带前缀的 model 路径实测 404 系路径错误，**不构成「论文声称的仓库不存在」的反证**。
   **datasets 页面随后亦经浏览器实测核验**：64 案例行、regime 列 8 个枚举值、
   question_family 枚举（actor_identity / principal_authority 等）与八个治理属性维度
   一致，许可 CC-BY-4.0。**至此 Zenodo、GitHub、Hugging Face 三处存档全部核验闭合。**
3. **一处如实登记**：Zenodo 记录标题（"A Decision Evidence Maturity Benchmark for
   Agent-Runtime Decisions Across Evidence Regimes"）与 arXiv 论文标题（"A Cross-Regime
   Benchmark for Agent-Runtime Governance-Evidence Sufficiency"）不一致，属存档与论文
   常见差异，已在 REFERENCES 条目注记。
4. **版本号 2.0.0 → 2.0.1**：`VERSION`、README 两版、`CITATION.cff` 共 10 处同步。

## v2.0.0（2026-09-02）— 23 条登记备用全部回灌：备用清单清零，双向引用检查完全闭合

1. **【登记备用】清零（23 → 0）**：v1.8.0 存量的 13 条（含 arXiv 号）与 v1.9.0 新标的 10 条
   （无号）全部回灌至 `spec/` 正文并撤除备用标记。门禁第 24 项实测 `已声明引用键: 56 / 56 条`、
   `显式登记备用: 0 条`——REFERENCES 声明的每一条来源都真实支撑正文，隐性过度宣称清零。
2. **回灌落点分组**（按章，核心论点各一句）：
   - **总纲一（01，中英）**：SemaClaw（arXiv:2604.11548）——「模型能力趋同、harness 层成为
     架构差异化的首要场所」，为本体系重心判断提供独立得出的定义侧论据；AutoHarness
     （arXiv:2603.03329）——harness 收益可量化（Kaggle GameArena 败局 78% 归因非法走子、
     145 个 TextArena 消除全部非法走子），但其极限形态（策略编译为代码、决策时刻不调用 LLM）
     同时使运行时监察失去对象。⚠️ 78% 口径 = 「败局中归因于非法走子的比例」，不得混用。
   - **威胁模型章（06，中英）**：Hugging Face 官方技术时间线（受害方视角三处细节）+
     Zaidi（SSRN）学术案例研究——事故定性为「规范博弈在安全护栏被刻意放松下升级为未授权
     入侵」，且现有监管框架未覆盖 agentic 问责缺口与身份缺口。据此登记**未解问题 #27**。
   - **蜂群治理章（07，中英）**：2505.02077（multi-agent security 学科定位——自由形式协议
     使秘密共谋成为可能、网络效应放大扩散）；2601.10599（Institutional AI——agentic 对齐
     漂移：单独对齐的 Agent 经交互动力学收敛到共谋均衡，单 Agent 审计看不见）；SWARM
     （arXiv:2604.19752）——软概率标签 + 四类治理杠杆（交易税 / 熔断器 / 声誉衰减 / 随机
     审计）；Kusne（*Communications Materials* 7:173）——自主材料实验室为多 Agent 协作的
     正当价值提供实证。收束段：使协作有价值的性质与使它难审计的性质是同一批——取舍点定在
     哪里登记为未解问题，误判代价是结构性的而非执行偏差。
   - **证据完整性章（08，中英）**：Solozobov Decision Trace Schema（arXiv:2604.09296）——
     八维度是**不可随档位下调的下限**，采集深度按决策风险分层（lightweight / sampled /
     full）；AI Sandboxes（arXiv:2606.18532）威胁模型与测量框架并入沙箱治理感知。
   - **运行监察章（09，中英）**：Dzhaliuk 等（*Int. J. Information Security* 25(4):109）——
     30 万条标注基准上**专用提示注入检测器召回率反而低于通用分类器**：「更专用」不蕴含
     「更可靠」，检测效果是部署取舍的函数。⚠️ 摘要无数值，均衡基准不得外推生产选型。
   - **机制溯源表（11，中英）**：SABSA（IEEE IoT Journal Early Access，DOI
     10.1109/JIOT.2026.3717946）——以 IaC 扫描实现多层架构可追溯性，证明**溯源关系可以
     是流水线的自动产物而非人工编制文档**；对照本表当前人工编制、无自动校验的现实缺口，
     登记**未解问题 #28**，表内新增「溯源关系自动核验（概念采纳）」一行。
3. **SABSA 据实降级 B → C**：唯一实证内容（DistilGPT-2 微型模型 + 模拟对抗向量 + 41.6 ms）
   受三重限定（Early Access 未定稿分页 / 实质域迁移 / 只覆盖直接提示注入），任何数值不可
   引用，仅作概念采纳。同步**删除原落点中「间接提示注入才是本体系威胁模型的核心」**——
   spec/06 并未把提示注入单列为条目，该表述无据，属于引用落点替正文立论，已更正。
4. **未解问题 26 → 28**：#27（评估环境的隔离边界由谁定义与核验——OpenAI / ExploitGym /
   Hugging Face 三方互不相干且无人对边界负责；L1 当前只约束任务内容）与 #28（机制溯源表
   自身的可核验性——最低档静态一致性检查 vs 高档机制级存在性检查，待裁决）。README 两版
   同步为 28 条；门禁第 9 项按实测值校验通过，无需改脚本。
5. **门禁分级计数改版**：B:46 / C:8 → **B:45 / C:9**（SABSA 降级所致）。`verify_consistency.py`
   硬编码断言与 README 两版字符串断言同步；改版后门禁 28 项全绿。
6. **数据订正（举证记录）**：Dzhaliuk 条目原登记标题缺尾词「attacks」、缺卷期与收稿 / 接受
   日期，已按 Springer 与 ACM 双源补齐；AutoHarness 日期 2026-03 → **2026-02-10**；
   SemaClaw 补全 11 位作者全名。三条订正均以出版方页面为准。
7. **版本号 1.9.0 → 2.0.0**：`VERSION`、README 两版（badge / 当前版本 / APA 引用）、
   `CITATION.cff`（version ×2 + date-released）共 10 处同步；CHANGELOG 历史条目与脚本
   注释中的历史性提及不改。

## v1.9.0（2026-09-02）— 引用键泛化：悬空检查覆盖全部 56 条，无号条目不再是盲区

1. **门禁新增第 26 项（引用键的声明与质量）**，三条子规则：
   - **(a) 完备**：无 arXiv 号的条目必须在文献列显式声明 `【键: XXX】`。不声明则该条目
     永远进不了第 24 项的比对，盲区会复现。
   - **(b) 唯一**：键不得与其他条目的键撞车。撞车则命中无法归因到具体条目，等价于没有键。
   - **(c) 非通用**：键不得命中黑名单，且长度 ≥ 4 字符。
2. **覆盖面 40/56 → 56/56**：v1.8.0 的第 24 项只能比对含 arXiv 号的 40 条，
   剩下 16 条（官方博客 / 技术报告 / 期刊论文）是盲区。本轮无号条目全部声明键后，
   并入第 24 项的同一规则——**撤掉其【登记备用】即被第 24 项拦下**（反向验证 T4 已证）。
3. **16 条无号条目逐条处置**：
   - **6 条确被引用**（键命中 `spec/` 正文）：Anthropic 2026-04 事件（`Glasswing`，6 处）、
     Teichmann（`Teichmann`，4）、OpenAI 官方博客（`GPT-5.6`，2）、OpenAI 技术报告（`898 题`，4）、
     METR / Redwood（`METR`，20）、CAR（`HarnessCard`，4）。
   - **10 条真悬空**（键在正文零命中，标【登记备用】）：Zhiyuan Wan 容器沙箱挖掘、
     Dzhaliuk 提示注入检测比较、Kusne 自主材料实验室、GEN-RWD Sandbox、Mahmutovic EU AI Act、
     Aggarwal SABSA、Gonzalez Torres 监管沙盒与 MLOps、Buscemi Sandbox Configurator、
     Zaidi（SSRN）事故案例研究、Hugging Face 技术时间线。回灌留待后续按落点聚类处理。
   - 显式【登记备用】总数由 13 条增至 **23 条**，全部在门禁输出中逐条公示。
4. **键选取标准写入 `STYLE.md`**（新增「引用键选取标准」一节）。核心结论与教训：
   **键是人对引用关系的声明，不是脚本可推断的属性。** v1.8.0 摸底时两次以自由关键词判定命中，
   两次都是误报——正文里的「EU AI Act」指向的是 Janssen 与 Nannini 而非 Mahmutovic 那篇；
   「Hugging Face」的 5 处命中都是把它当事故当事方提起，而非引用其技术时间线。
   故本轮键统一取**第一作者姓氏或该文献独有词元**。
5. **第 26 项反向验证 6/6**：基线全通过；T1 删键、T2 键撞车、T3 改成黑名单通用词（`EU AI Act`）、
   T3b 改成过短缩写（3 字符）、T4 撤掉无号条目的【登记备用】（证明已并入第 24 项规则）
   ——五组缺陷**全部被拦下**。验证在临时副本上做，原仓库零改动。
6. **溯源信息补全，16 条无号条目的 DOI / URL 覆盖由 12/16 升至 16/16**。三条 DOI 经检索核实后补入：
   - Zhiyuan Wan 等，容器沙箱挖掘：`DOI 10.1007/s10664-019-09737-2`，
     *Empirical Software Engineering* 24(6):4034–4070（补卷期页码与全部四位作者）。
   - GEN-RWD Sandbox：`DOI 10.1186/s12911-024-02549-5`，
     *BMC Medical Informatics and Decision Making* 24(1):170（补卷期页码，作者实为 9 位）。
   - Mahmutovic，EU AI Act：`DOI 10.1093/ijlit/eaaf028`，
     *International Journal of Law and Information Technology* Volume 33, 2025, eaaf028。
   - Anthropic 官方红队博客链接补 `https://` scheme——原串无 scheme，正则与点击均无法识别。
7. **一处日期更正（举证记录）**：Mahmutovic 条目原标「2025-12-24」，检索未获任何来源支持；
   以 Oxford Academic 官方页核实到的「Volume 33, 2025, eaaf028」取代。
8. **`STYLE.md` 未解问题编号区间更正**：原写「#1–#15，新增从 #16 起」，与 `spec/13-boundaries.md`
   实测 26 条不符（门禁第 9 项长期按实测值校验，故未报错）；改为「#1–#26，新增从 #27 起」。

## v1.8.0（2026-09-02）— 悬空引用根治：门禁双向引用检查 + 16 条存量逐条处置

1. **门禁新增第 24 项（REFERENCES 无悬空引用）**：含 arXiv 号的条目，须在 `spec/` 正文出现该号；
   确系有意登记而未回灌者，须在落点列显式标注【登记备用】。备用清单在门禁输出中**逐条打印公示**，
   无法静默堆积——显式标记是可审计的豁免，不是后门。
2. **门禁新增第 25 项（反向悬空）**：`spec/` 正文引用的每个 arXiv 号，均须在 REFERENCES 有条目登记。
   与第 24 项对称，双向都查，REFERENCES 与正文的引用关系才真正闭合。
3. **两项新检查均做了反向验证**：第 24 项 8/8（含「标记全加→转 PASS」「只加 n−1 条→剩余仍被拦下」
   「删除正文已锚定号→新报错」「【备用】/【登记备 用】等近似标记不得豁免」）；第 25 项 4/4
   （含「删除 REFERENCES 中的号」「正文凭空引用未登记号」与「正文去掉引用后由第 24 项接管、不误报」）。
4. **全仓扫描结论**：56 条来源中含 arXiv 号者，有 **16 条在 `spec/` 正文零命中**——
   REFERENCES 声称它们支撑某章，正文里根本没有。这是**隐性过度宣称**，与证据完整性章自己写的
   「错觉比缺失更危险」属同一类问题。
5. **回灌 LongCoT（arXiv:2604.14140）→ 任务准入与分级一章**：确立「任务跨度不因模型能力而豁免」——
   该基准的关键构造是**每个局部步骤对前沿模型都可单独求解**，故失败只能归因于长程推理本身。
   ⚠️ 只否证「模型能力可替代对任务跨度的考量」，**不得**据此断言「有 harness 也解决不了长程任务」。
6. **回灌 A2P Scaffolding（arXiv:2509.10401）→ 证据完整性一章**：事后归因存在经验天花板，
   最优方法在更难的**手工构造集上仅 29.31%**（较易的算法生成集为 47.46%），
   故证据必须在动作发生时采集完整。⚠️ 引用口径**须取 29.31%**，不得以 47.46% 表述该方法实际水平。
7. **回灌 From Prompts to Contracts（arXiv:2607.08028）→ 运行监察一章**：补上
   「约束必须由代码所有的 harness 承载」的另一面——外挂式 guardrail 的失效形态**不是拦不住，
   而是过度拒绝**（效用 120/120 → 88/120）。据此确立：强制层必须**拦阻率与效用并重**考核，
   只考核拦阻率会系统性奖励过度拒绝。⚠️ 附四项限定（单机构自实现 / 单一领域 / 自设 120 分制不可外推 /
   比较对象非本体系 L2）。
8. **其余 13 条显式标注【登记备用】**并在门禁输出中逐条公示（AI Sandboxes、AI Sandbox 技术报告、
   GPAI 风险源、AI Code Sandboxes、Cyber-Capable AI Agents、AutoHarness、
   Open Challenges in Multi-Agent Security、SWARM、Institutional AI 立场论文、
   Agent Safety Should Be a Runtime Contract、SemaClaw、Decision Trace Schema、AIREP）。
9. **修正唯一一条反向悬空**：AIprint 条目长期漏写 `arXiv:2607.14434v1` 与 DOI，
   而机制溯源表一直在引用它——已按 arXiv 元数据补齐编号、DOI 与全部 7 位作者。
10. **REFERENCES 元数据补全**：LongCoT、A2P、From Prompts to Contracts 三条补齐作者全名 / 精确日期 /
    DOI / 代码仓库；Institutional AI 两篇（2601.11369 实验论文、2601.10599 立场论文）补全作者与版本日期，
    并在立场论文条目加注**区分说明**——两篇作者群体高度重合、提交日期相邻，**不得**混为一谈。
11. **机制溯源表双语各 3 行**，与上述三条回灌一一对应，依据列均带限定语。
12. 版本 1.7.0 → 1.8.0。本轮**未新增 REFERENCES 条目**，来源合计仍为 56 条（A:1 / B:46 / C:8 / D:1），
    未解问题仍为 26 条。

## v1.7.0（2026-09-01）— 吸收 P1 第五批：留痕≠可归因的第二条证据线 + 审计即查询 + 披露卡监管侧依据

1. **spec/08**：为「留痕不等于可归因」补上**第二条独立证据线（法律侧）**——From Runtime Records to Legal Findings（arXiv:2607.00941，Jeroen Janssen，2026-07-01，DOI 10.48550/arXiv.2607.00941；12 页技术报告，辅助材料与实验协议存于 Zenodo，DOI 10.5281/zenodo.21025237）。此前该结论只有 DEMM-Bench 一条证据线，且其 64 个案例由作者构造；本条从法律认定的角度独立提出同一结论，两者路径无关。准则为：一条运行时记录要能回答一项二元事实认定（受保护数据是否越界 / 人是否本可介入 / 信息隔离墙是否成立 / 授权在使用时点是否有效），须**同时**携带**类型标注（typing）**与**判定真值所依赖的关系**（来源 / 授权 / 推导 / 时间效力），二者缺一即不足以支撑认定。
2. **作者自陈为必要性主张、非充分性主张**：满足准则不等于认定成立，不满足则认定一定不成立。该文并论证仅靠防篡改日志、通用过程框架或来源结构本身都不足以建立认定，并把论证接到 requisite variety、Good Regulator Theorem 与 trace-vs-hyperproperty 的边界。⚠️ 因此**不得**表述为「满足该准则即可归因」；该文实例化于作者所选定的部分欧盟 AI Act 监督义务，属**作者的法律分析、不代表监管方立场**，**不得**表述为「欧盟 AI Act 要求」。
3. **澄清一个易犯的错误**：Quipu 虽独立跑通了 DEMM-Bench 并复现同类结论，但它跑的是**同一套基准**，属第一条证据线的**外部使用者**，不构成第三条独立线——与本规范「同一条证据线不得加总为两条」的处置规则一致。本体系此处共**两条**独立证据线，不是三条。
4. **spec/08**：新增「审计即查询：把复核从周期性人工活动变为一次查询」——以 Quipu（arXiv:2608.16813，Steve Brown，2026-08-17）为已实现样例：把治理规范 Σ、执行轨迹与经签名的判定本身作为其所治理存储中的事实，于是 T ⊨ Σ 成为一次查询。其反转的四个知识图存储默认中，**第二条（数据、信任标签、判定与规则本身皆双时态）直接解决上一节提出的问题**——「用昨天的日志对照今天的规则」变成一次 as-of 查询，而不再是静默污染；Census 给出演示：50 / 50 判定在其各自时点忠实重算，而 latest-only 规则下 50 条全部误报。
5. **三项引用限定必须一并保留**：Census 为**确定性单次种子化运行**，0 / 6、7 / 7、50 / 50 均为按构造的自评结果，**不得**外推为真实系统的实测频率；**该文作者即该存储的实现者**，评估属自评其自有实现，存在利益关联，只可作可行性证据与检查清单，不可作品质背书；该存储是**存储层**方案，与 E 子系统是上下游而非替代关系——采集层不可信时，存储层的任何保证都不成立。本体系不指定存储实现。
6. **spec/13**：为 harness 披露卡的必要性补上**第三条、且来自外部的独立依据**——AI Agents Under EU Law（arXiv:2604.04604，Luca Nannini 等 9 位作者，2026-04-06，DOI 10.48550/arXiv.2604.04604；Working Paper）：其结论「提供者的基础性合规任务，是对 agent 的外部动作、数据流、所连接的系统与受影响的人做穷尽清点」与本卡内容高度重合（外部动作与连接系统对应字段 4–5，数据流对应字段 3 与字段 4 的接口模式部分）。此前披露卡的必要性理由**全部来自本体系内部**。⚠️ 该文是作者的监管映射分析，**不代表监管方立场**，只可表述为「一份第三方系统性映射工作文件论证……」，不得表述为「欧盟 AI Act 要求」；只作旁证，不改变本卡「披露要求而非技术门槛」的定位。
7. **消掉第二例悬空引用**：Janssen 条目此前已在 REFERENCES 中登记，落点写为「与 DEMM-Bench 并列的第二条独立证据线」，但全仓 spec 正文对该文**零命中**——概念只存在于 REFERENCES，从未被回灌到任何执行条款。本轮写入证据完整性章正文。（第一例为 v1.6.0 中的「配置权即攻击面」。）
8. **未解问题 25 → 26**：#26 harness 披露卡是否应增设「受影响方」字段。该文列举的第四类对象「受影响的人」在本卡 11 个字段中**没有专门承载者**——字段 11 讲系统自身的局限与风险，字段 7 讲权限与审计支持，都不回答「动作作用到谁身上」。⚠️ 措辞刻意**不留后门**：写明「在判定之前，本条不得作为推定任何系统不合规的依据」。
9. **REFERENCES**：新增 AI Agents Under EU Law 条目（B 级，补齐 9 位作者、日期、DOI 与 Working Paper 限定）；Janssen 条目补齐全名、日期、DOI、Zenodo 存档，并写明回灌落点与两项限定；Quipu 条目补「审计即查询」落点，并注明其属同一条证据线的外部使用者。**合计 55 → 56 条**（B 级 45 → 46）。
10. **spec/11** 机制溯源表双语各 +3 行，逐条标注证据等级与限定语。
11. **门禁反向验证 6/6**：本轮改动了门禁中四处计数阈值（合计条数、B 级实测、README 分布、注脚），已逐项做「故意破坏 → 确认报错 → 还原」验证，还原后 23/23 全 PASS。
12. **版本 1.6.0 → 1.7.0**（VERSION / README 双语各 3 处含 badge 与 APA 引用 / CITATION.cff 两处）；README 双语未解问题条数 25 → 26、来源条数 55 → 56、B 级 45 → 46。

## v1.6.0（2026-09-01）— 吸收 P1 第四批：交白卷第二条证据线 + 配置权即攻击面

1. **spec/04**：新增「交白卷的第二条证据线：难点在『何时』而非『能否』」——Agentic Abstention（arXiv:2606.28733，Han Luo / Bingbing Wen / Lucy Lu Wang，2026-06-27；13 个 LLM-as-agent 系统 + 2 个 scaffold、**超过 28,000 个任务**，覆盖网页购物 / 终端环境 / 问答三类环境）。此前唯一的证据线（AgentAbstain）是 263 组配对任务、42 个沙箱，规模与生态位都受限；本条在两者上都不同，构成第二条独立线。
2. **三项发现各自的落点**：①**主挑战不是「能否弃权」而是「何时弃权」**——独立印证本规范新增的第四信号「白卷时机」，并补上了它的高发场景：**指令看似可行、直到环境揭示并非如此**（例如没有任何有效结果与指令匹配），这正是事后弃权的高发场景，为把此类行为计为加重情节提供了独立的场景证据。②**更大或更强的模型在及时弃权上有时反而更差**——这是「弃权能力不随通用能力自动提升」的第二条独立证据（第一条是 φ 系数反相关）。③**CONVOLVE 不更新任何模型参数，即把 Llama-3.3-70B 的及时弃权召回由 26.7 提到 57.4**——这在本体系里不是一条效率数据，而是一条路线判断：**「及时弃权」是一个可以在 harness 层（停止规则、上下文工程、任务定义）被工程化改善的对象**，因此把治理重心放在任务定义层与运行监察层是有回报的，而非只能等待模型对齐的进步。
3. **由发现②产出的两条执行要求**：其一，**白卷率基线在模型升级或换型后必须重新标定，不得沿用旧基线**；其二，**不得把「换一个更强的模型」当作改善弃权表现的手段**——否则部署方会系统性地选出「更能干但更不肯认输」的模型，而后者正是本体系要防的失效模式。
4. **三项限定必须一并引用**：26.7 → 57.4 只在 WebShop 上、只在一个模型（Llama-3.3-70B）上验证，不得外推为通用增益、不得作为生产预期；「更强模型更差」的原文是 **sometimes（有时）**，不得表述为「模型越强、弃权越差」；**两个来源的「及时弃权」口径并不相同**（该文指「在过多不必要的交互之前停下」，本体系指「在不可逆动作之前声明弃权」），不得把两者数值直接合并。
5. **spec/02**：新增「谁有权修改规则」**第四条 —— 配置权即攻击面**。依据 HarnessRisk（arXiv:2608.17597）：三个 harness 的六阶段生命周期中**配置阶段最脆弱**；而成因不是配置权限太大，恰恰相反——**已授权的配置编辑**可以把不安全参数（明文凭据内联 / 共享范围扩大 / 审批步骤关闭 / 网关公开化 / 脱敏弱化）藏进一条**本来就被 sanction 过的工作流**。此类变更在性能上完全可以是中性甚至为正：关掉一个审批步骤会提升通过率、不会触发任何回归告警，因此 g(c) > 0 与 R(c) ≤ δ 两道判据都会放行。由此确立判据：**性能不回归不等于安全不退化**——变更准入须在准入判定式之外，并行一道**配置内容审查**。
6. **⚠️ 消掉本体系两处悬空引用**（本轮最有价值的发现）：蜂群章「对抗适应」段与未解问题 #19 都写着「与**监察章**『配置权即攻击面』同源」，但 `spec/09-oversight` 双语**零命中**——这个概念此前只作为 REFERENCES 里 HarnessRisk 条目的一个落点标签存在，**从未被真正写入任何正文**。本轮把它写入架构章（L1 侧，与「谁有权修改规则」同处一节，因该节第一条即规定变更须经 L1 复核），并把两处引用的「监察章」改为「架构章」。这正是回灌清单②要抓的「悬空」形态：条目登记了，但没有回灌到执行条款。
7. **未解问题 23 → 25**：新增 #24（配置内容审查的**判定标准与自动化**——五项审查项目前是清单而非可执行判定式，且与准入判定式如何合成未定；⚠️ 明确写入：判定式落地前本节仍具规范性，五项须人工逐项过，**不得**因「无自动化手段」而跳过）与 #25（「及时弃权」的**口径统一与直接测量**——本体系的白卷时机目前没有直接可比对的外部测量；⚠️ 在测量落地前，白卷时机**不得**单独作为扣分依据，只能与率值、信息量两个信号合并使用）。两处都刻意**不留后门**——未因「尚无手段」而给执行留豁免。
8. **回灌清单②的一处接线**：spec/04 产出的「模型选型不得只按通用能力榜单排序」已同步到架构章 L1-部署方的责任主体说明（选型须含弃权能力等独立项，且模型升级 / 换型后白卷率基线须重新标定），避免结论只落在产出它的那一章。
9. **REFERENCES.md**：HarnessRisk 与 Agentic Abstention 两条补本轮新落点；并**修正 Agentic Abstention 条目的两处不精确**——补齐完整标题（*…Do Agents Know When to Stop Instead of Act?*）、三位作者姓名、精确日期（2026-06-27）与 DOI，并把原条目中「更强、更大的模型在及时弃权上反而更差」补回原文限定 **sometimes**（原写法漏掉限定，构成过度宣称）。参考文献总数不变（55 条，其中 B 级 45 条）。
10. **spec/11**：新增 2 条机制行，逐条标注证据等级与限定语；「配置权即攻击面」一行同时标注了它此前是悬空引用。
11. **版本 1.5.0 → 1.6.0**；README 中英双语 7 处未解问题条数声明 23 → 25，由门禁自动核验同步。

## v1.5.0（2026-09-01）— 吸收 P1 第三批：分级章补强（可逆性判据 / harness 成熟度第三轴）

1. **spec/03**：新增「可逆性：从隐式措辞提为 G 轴的显式判据」。可逆性在本文件里原本出现三次却从未被命名——G4 定义把它与人身安全并列、定级主体与裁决一节提到「处置动作不可逆时」、决策树 Q3 写成「变更可完全回滚且影响面可控」。三处都在用它，没有一处把它当作独立判据。现提为三值分类判据：**完全可逆**（不单独抬升 G 档，仍受可验证性与外部性判据约束）/ **部分可逆**（**抬升至不低于 G3**，且强制在执行前留存并固化回滚点）/ **不可逆**（**直接定为 G4**，处置动作进入人类终裁）。
2. **为什么是三值分类而不是连续量**：外部 OCM 路由模型（arXiv:2606.22484）把可逆性列为 {irreversible, partial, full} 三个**分类取值**而非连续量，路由靠优先级规则而非数值比较。沿用这一形态的理由是——「部分可逆」在治理上是一个无法用二分表达的独立类别：它恰恰是那些「技术上能回滚，但回滚不消除后果」的动作（已发出的邮件、已提交的公开记录、已消耗的外部配额），而这正是最容易被「能回滚所以低风险」这一直觉漏掉的一类。
3. **为什么只取这一维**：该路由模型共有四维——监管影响 / 客户贴近度 / 可逆性 / 数据敏感性。本框架只采纳可逆性：**监管影响**属合规义务范畴，不由任务定级承担，且该来源的监管映射自陈未获任何监管方验证、**不得引用**；**客户贴近度**描述受影响主体是谁，本框架由「外部性」判据（Q1 / Q4）承担同一功能；**数据敏感性**由保密分级机制承担，不是 G×S 的因子；**可逆性**描述的是**动作本身能否被撤销**，上述三者都无法替代——一个不涉敏感数据、不涉外部客户、无合规义务的内部操作，若不可逆，依然需要兜底。⚠️ C 级：**只取维度概念本身，不取该来源的任何数值**（四维均为分类域而无阈值、唯一数值参数无实证校准、形式性质仅 "by construction"、无代码与数据制品）。
4. **spec/03**：新增「harness 可观测性成熟度 H0–H3：G×S 能否落地的前提」。G×S 分级隐含了一个从未言明的前提——部署方 harness 具备履行该档监察强度所需的可观测性；而这个前提可以为空：一个只把「任务描述 + 仓库文件」交给 Agent、最后只收回一个产出物的 harness，提供不了 G2 以上所要求的抽查、资源账本与证据固化所需的任何素材。现把该前提显式化为第三根轴 H。三个性质一并采纳：**可见性是单调的**（高级别不隐藏低级别已暴露的东西，升级 harness 不损失既有可观测性）；**评估方的隐藏注释对 Agent 永不可见**（可观测性单向，在对抗场景下不会变成一条泄漏通道）；**H3 把「完成」变成一个证据对象**而不只是状态位，与证据完整性子系统的目标直接对齐。
5. **H 与 G×S 的关系定位为「不是并列的第二套分级，而是 G×S 能否落地的前提条件」**——二者正交：G×S 度量任务的风险，H 度量部署方 harness 提供可观测性的能力。给出 G → H 耦合下限（G0/G1→H0、G2→H1、G3→H2、G4→H3）：部署方 harness 低于下限，则**该任务不得按该 G 档执行**——要么先把 harness 补到下限，要么按低一档降级并显式记录降级理由。此处**不得用其他维度的高分补偿**：G×S 既有的「取高」规则本身就是非补偿式的（不因某一维表现好而放宽另一维），这与外部在提出成熟度指数时主张采用「非补偿式瓶颈加权」同源，可互为印证。
6. **两处限定必须一并引用**：其一，该阶梯的每一级外部**只跑了 1 次（共 4 个 episode）**，作者明确说明不用于总体层面的比较，**不得据此声称「某一级一定更好」**；其二，上表 G → H 的映射是本框架的设计推演而非外部实证，其标定登记为 **未解问题 #23**（并显式写明：标定未完成不改变该表的规范性——它是定级约束，只是档位映射待验证；不因证据等级为 C 而给执行留豁免）。
7. **⚠️ 修掉决策树的一处真实缺陷：无出口路径**。原树在「不可完全验证 → 不可完全回滚 → 无外部副作用 → 非人身安全 / 非不可逆」这条路径上**没有任何输出**——任务走到 Q5 的「否」分支后无档可落。现由 Q5 收口：影响面可控 → G2，不可控 → G3。同时拆开原 Q3：它把「变更可完全回滚」与「影响面可控」两件事混在一句里，现改为**可逆性归 Q3（三值）、影响面归 Q5（二元）**。并新增 **H 轴**——定级输出由 (G, S) 变为 (G, S, H)，且须校验 H 是否满足该 G 档的下限。
8. **未解问题 22 → 23**：新增 #23（G → H 耦合下限的标定），并写明标定路径（先建立 H 级的独立度量口径，再按 G 档逐一检验「低于某 H 级时该档监察强度的哪一环先失效」）。README 中英双语 7 处条数声明由 v1.2.1 新增的门禁检查自动核验同步。
9. **spec/11**：新增 2 条机制行（可逆性判据、H0–H3 成熟度轴），逐条标注证据等级与限定语；其中 H0–H3 一行显式标注「每级仅 n=1，只作框架依据」。
10. **REFERENCES.md**：给 AI Harness Engineering（arXiv:2605.13357）与 GAIE（arXiv:2606.22484）补本轮新落点——前者由「L2 检查清单的候选骨架（待评估是否与 G×S 分级冲突）」变为已采纳的第三根轴 H，并回应了原条目留下的「是否与 G×S 冲突」疑问（定位为前提而非并列分级，因而不冲突）；后者由「『可逆性』作为分级因子的概念候选，尚未采纳」改为**已采纳可逆性、其余三维不予采纳**，并写明不予采纳的理由。
11. **门禁再次拦下一处缺陷（提交前）**：插入 #23 时行尾漏了竖线，被 v1.4.0 新增的「表格每行列数与分隔行一致」检查当场拦下（报「列数 3 != 分隔行 4」），修正后 23 项全部 PASS。同时按本文件既有风格把 spec/03 新增三张表的数据行归一为不带首尾竖线的形式（该文件原为 2 张不带 / 1 张带，现该段落内统一）。
12. **回灌清单②的两处接线**（新增第三根轴后回头补的）：spec/13 的 harness 披露卡「为什么必须有它」一段补上 H 轴的素材依赖——H 逐级依赖字段 4 + 6（H1 的工具与测试注册表）、字段 2 + 11（H2 的 Agent 指南、架构说明与已知缺陷清单）、字段 6 + 8 + 9（H3 的确定性检查、重放支持、失效分类与验证协议），G 轴的可逆性判据则依赖字段 3 的回滚策略与字段 4 的外部接口面；README 中英双语的文件索引描述与上手引导第 2 步补上 H 轴与 G → H 耦合下限校验。

## v1.4.0（2026-09-01）— 吸收 P1 第二批：架构章三合一（权限边界 / 四段式 / 预算锚点）

1. **spec/02-architecture**：新增「L2 预算的第一个经验锚点」——LongHorizon-Harness（arXiv:2608.01964）的 token 分解给出审计者占比 19.4% / 24.8% / 38.1%，本体系的「L2 ≈30%」落在 19–38% 区间内，**首次具备可证伪的形式**（长期越界则主张需修正）。三点限定一并写入：口径是 token 而非组织成本；**越弱的执行者，独立监察越贵**（较强模型总 token −33%，较弱模型 3.2×），故支持按 G×S 分级差异化配置监察强度而非统一配额；存在反例（Terminal-Bench 上加了独立监察反而少 24%）。⚠️ 须挂 COI 声明。
2. **spec/02-architecture**：新增「监察角色的权限边界：三角色分离」——把「执行者不能自我认证」从原则落成可验收的权限表（管理者无任何环境接口 / 执行者是唯一可有意修改环境的角色 / 审计者只读）。三条硬约束中，「状态更新只认环境验证过的事实」是「不依赖一号文书」的工程形式——不是「不信一号的自述」，而是**在结构上让自述不具有证明力**；「审计期间的任何变更记为完整性违规」补上了 E5 此前未覆盖的一面：采集点不能被动手脚之外，**被采集对象在采集窗口内也不能动**。
3. **⚠️ 发现并登记本体系的一处内部缺口**：三角色设计的第三条（跨轮记忆只有审计报告、执行者原始轨迹逐轮丢弃）与本体系 L3 的「离线重放」存在**真实张力**。裁决为两条留存链路分离，但并未闭合问题——E3 固化的是内核侧采集到的事件（E1），不是 Agent 的推理轨迹；前者能回答「它做了什么」，后者才是「它为什么这么做」。登记为 **未解问题 #21**。
4. **spec/02-architecture**：新增「执行者不能自我认证的工程落地：四段式契约」——采纳 SDB（arXiv:2605.20173）的 proposer / verifier / commit step / reject signal 作为可验收的接口定义，以「5 个主流框架 21 个调用点中 **19 个已有显式 verifier-and-commit**」这一**描述性**事实为支撑；并确立「**静默丢弃不构成第四段**」（verifier 不通过必须回传可操作的拒绝信号，否则 proposer 无法据此修正）。
5. **spec/02-architecture**：登记新失败模式 **重放分歧（replay divergence）**——同一份事件日志在更新的模型版本或变更后的提示下重放，会产生与首次运行不同的下游输出。⚠️ **该失败模式只有命名与诊断程序，无任何实验、数据集或数字**，按证据纪律只作**概念采纳**，不声称其已被证实。它暴露了 L3 离线重放的一处未言明前提（「同一份记录 + 同一次重放 = 同一个结论」），而验证谓词本身具有模型版本依赖性。当前处置仅为缓解：重放时锁定模型版本与提示并把二者记为必需元数据；未解部分（已归档的重放结论是否需随模型升级重新出具）登记为 **未解问题 #22**。
6. **REFERENCES.md**：新增 CASE（arXiv:2608.10153，Srinivas Telukunta、Georgios Nektarios Lilis、Lucio Baron，2026-08-10）——三项实证（82% 的已记录生产环境失效为多层级轨迹 / 22 个生态工具无一提供完整的涌现层覆盖 / 35 个经打分的公开部署全在最低成熟度档），援引必要多样性定律论证无辅助的人类监督在结构上就会失败，作 L2 预算与交白卷机制的方向性背书。⚠️ 本条新收录、尚未走独立技术复核流程；其「Layer 2（涌现）」为该文自身的分层命名，与本体系 L2 的重合是**命名上的对应，不是已验证的等价**。LongHorizon-Harness 与 SDB 两条补新落点。
7. **spec/13-boundaries**：未解问题总数 20 → **22**，README 中英双语 7 处由 v1.2.1 新增的门禁检查自动核验同步。
8. **spec/11-traceability**：新增 4 条机制行并逐条标注证据等级，其中「重放分歧」一行显式标注为「概念采纳，非实证」。

9. **修复 v1.3.0 引入的渲染缺陷**：`spec/11-traceability` 双语各 4 行（A 块新增的蜂群四行）实为 6 列，而表头只有 5 列——**多出的第 6 格会被 Markdown 渲染器静默丢弃**，即那四行的 ⚠️ 限定语在发布版本中不可见。已把第 6 格并入「依据」列，与其他行的写法一致。
10. **门禁加固（两项漏检已补）**：
    - **表格分隔行检查此前只覆盖一种风格**。本仓库存在两种表头写法——带首竖线（`| 表头 | … |`，13-boundaries 等）与不带首竖线（`表头 | …`，02-architecture / 11-traceability / REFERENCES 等）。旧判据只按「以竖线开头的连续行」识别表格块，导致第二种风格下整个表格被识别为单行块而**完全跳过校验**。现已统一按「含单元格分隔的连续行」识别，两种风格都覆盖。
    - **新增「表格每行列数与分隔行一致」检查**，即第 9 条的缺陷类型。两项均做了反向验证：拆除分隔行 → 门禁报 `表头后缺分隔行`；注入多余单元格 → 门禁报 `列数 6 != 分隔行 5`。
11. **门禁分级计数正则修正**：`REFERENCES.md` 的 A/B/C/D 计数原先使用四个不一致的正则（`^A \|` / `^B \|` / `^C（` / `^D（`），**带括号说明的 B 级行会被漏计**（本轮新增的 CASE 条目即因此一度未被计入）。已改为统一正则 `^(A|B|C|D)(（…）)?\s*\|`，并同步各处声明：来源总数 54 → **55**（B 级 44 → **45**），涉及 README 双语 3 处、REFERENCES 注脚 1 处、门禁自检 4 处。`CHANGELOG` v1.2.0 条目中的「54 条」记录当时状态，保留不改。

## v1.3.0（2026-09-01）— 吸收 P1 第一批：蜂群章补强

1. **spec/07-swarm-governance**：新增「蜂群失效的三种形态」（协调失灵 / 冲突 / 共谋，Multi-Agent Risks from Advanced AI，arXiv:2502.14143）作为分类骨架，并暴露此前的覆盖缺口——本框架条款全部指向共谋一类，另两类无条款（登记为 未解问题 #20）。
2. **spec/07-swarm-governance**：新增「沟通层检测的盲区」（Colosseum，arXiv:2602.15198）——19 个开箱模型在良性提示 + 私有信道下 **57.89% 在行为上共谋，仅 10.53% 在沟通文本上被抓到**。据此确立「行为层与沟通层必须分别度量、有效性以行为层为准」，直接限定本章七个信号中偏沟通层的两个（协调协议出现、自引入密码学）。附四条缓解建议，均为规范性建议，**不引用其效果**。
3. **spec/07-swarm-governance**：新增「对策机制清单」（Knowledge-Based Systems 344:116067，DOI 10.1016/j.knosys.2026.116067）——五类人类反共谋机制映射，作为可逐项自检的完整性清单；其中「宽恕与举报」「市场设计」两类为部分覆盖，构成可认领缺口。
4. **⚠️ 既有论断降级（定位改写）**：蜂群章由「我能治理蜂群」改为「**我有机制映射 + 未解难题**」——现有产出是三类失效形态的分类骨架、五类对策的完整性清单与一份经限定的检测信号表，**不是「蜂群已被治理」的证明**。此为主张收敛（claim 减少）而非能力移除，故不计入破坏性变更。
5. **spec/13-boundaries**：新增未解问题 **#18**（Agent 身份流动性——身份可低成本再生时，信用分连带与任务分解树追责同时失效）、**#19**（对抗适应——检测信号是静态的，规则一旦公开即成为优化目标）、**#20**（协调失灵与冲突无治理条款）。总数 17 → **20**，README 中英双语 7 处由 v1.2.1 新增的门禁检查自动核验同步。
6. **spec/11-traceability**：新增 4 条机制行并逐条标注证据等级；本轮引用的两篇（综述类与映射类）均显式标注「不支撑某机制有效」。
7. **REFERENCES.md**：三条既有条目补充新落点，并对综述类与映射类补上「非一手实证」的引用红线。

## v1.2.1（2026-09-01）— 未解问题条数声明修正 + 门禁补漏

1. **修正已发布的事实错误**：v1.2.0 在 `spec/13-boundaries.md` 新增未解问题 #16、#17 后，`README.md`（4 处）与 `README.en.md`（3 处）仍声明「15 条未解问题」，与实际登记的 17 条不符。本次统一修正为 17 条。`CHANGELOG` 历史条目中「15 条」的表述记录的是当时状态，**保留不改**。
2. **门禁补漏**：`scripts/verify_consistency.py` 校验项由 19 项增至 **22 项**，新增第 9 类检查——从 `spec/13-boundaries.md` 实测未解问题条数，并校验 README / README.en 的声明一致。取数采用「连续自然数序列、中断即停」策略，避免把后续 harness 披露卡表格的 1–11 误计入。已做反向验证：将 README 改回 15 条后门禁正确 FAIL 并拦截。
3. **版本号同步**：VERSION / README（badge、当前版本、APA 引用）/ README.en（同三处）/ CITATION.cff 统一为 v1.2.1。⚠️ `CITATION.cff` 首行 `cff-version: 1.2.0` 是 **CFF 规范版本号，不是本框架版本，不随本框架版本变更**。

## v1.2.0（2026-09-01）— 吸收 harness 工程前沿调研

1. **spec/09-oversight.md**：新增「检测不等于阻断」——HarnessRisk（arXiv:2608.17597）显示在检出率高于 90% 时仍有可观的攻击成功率；检出率与攻击成功率相关 r = −0.708（p = 0.010），效用与攻击成功率相关 r = 0.299（p = 0.345，不显著——即「加强监察必然牺牲效用」缺乏统计支持）。确立三条设计要求：有效性指标须写为阻断率而非检出率；绩效表须同时报告召回率与攻击成功率；双盲考核考的是拦截而非识别。同节并存「配置阶段是 harness 生命周期中最脆弱的阶段」这一排序。
2. **spec/09-oversight.md**：新增「声明式禁令不具约束力」——Institutional AI（arXiv:2601.11369v2）三制度对照（无治理 3.10 / 仅提示词宪法 3.02 / 治理图执行 1.82，d = 1.28，Tier≥4 占比 50.0% → 5.6%），其中**仅提示词宪法在 GPT-5 Mini 上恶化到 3.60**。这为「看门狗必须是不依赖任何 AI 判断的确定性规则，而非写进提示词的高层原则」提供了最强、且来自无利害关系第三方的支持。同节记录该文 Oracle/Controller 分离、治理状态机含恢复态两处与本体系同构的结构。
3. **spec/02-architecture.md**：新增「谁有权修改规则」——补齐本体系此前缺失的立法权归属条款：修改权归属（自动改进系统不得直接写入生产 harness）、准入门槛分层（高风险变更需比通过率不回归更强的门槛，引 Self-Harness 作者原话为对方背书）、可复用准入判定式 g(c) > 0 且 R(c) ≤ δ（取自 DarwinX，arXiv:2608.07545）配套双划分验证与两阶段裁决。
4. **spec/08-evidence-integrity.md**：新增「留痕不等于可归因」——以 DEMM-Bench（arXiv:2606.20634）为方向性依据并附完整限定语（64 例为手稿构造、标签来自确定性判定器，75% / 50% / 56.25% **不得**作为真实系统实测频率引用），吸收其八个治理属性维度作为证据字段检查清单；新增「两类最易遗漏的留痕对象」——被阻止的尝试必须与已发生动作同等留痕并进入同一条哈希链，规则与契约变更须带语义摘要 + 字节摘要两级。
5. **spec/13-boundaries.md**：新增「接入前置披露：harness 披露卡」，采用 CAR（DOI 10.20944/preprints202603.1756.v1）的 HarnessCard，11 个字段已逐字核对于该文附录 Table A5，明确定位为披露要求而非技术门槛；新增未解问题 **#16**（harness 自修改的准入门槛与回归容忍上限 δ 未标定）与 **#17**（自动分级依赖元数据可信度，而元数据可信度本身无保障——吸收自 GAIE 的 L6 残余风险清单）。
6. **REFERENCES.md**：新增 28 条来源（B 级 21 条、C 级 7 条），全部含 DOI / arXiv 号，并对 C 级逐条标注引用红线（利益关联声明与不可外推限定）。分布更新为 **54 条参考文献（其中 B 级 44 条）**：A:1 / B:44 / C:8 / D:1。README、REFERENCES 注脚、校验脚本三处口径同步。
7. **scripts/verify_consistency.py**：校验项由 18 项增至 **19 项**，新增「引用红线」检查——已登记为禁止引用的数字，其字面值不得出现在任何正文文件中。首个登记项为 GAIE 中由 18 个自设参数推算出的解析估计值（数值本身与禁用理由见 REFERENCES.md 该条目的红线说明）。REFERENCES.md 自身豁免——那里是禁令的说明文字，不是引用。
8. **版本号同步**：VERSION / README（badge、状态行、APA 引用）/ CITATION.cff 统一为 v1.2.0；全部改动中英双语同步。

## v1.1.0（2026-08-31）— G×S 分级可操作性深化

1. **spec/03-grading.md 深化**（P0）：补齐定级主体与裁决路径（部署方自评 + 独立验证复核 + 人类终裁）、规模计数口径（并发活跃实例 / 并发峰值 / 子 Agent 计入所属任务 / 跨沙箱合计）、定级流程与重定级触发、证据等级标注（风险命题 B 级、阈值边界 C 级推演）；新增 G×S 决策树与强度矩阵，部署方可仅凭本篇完成一次定级。
2. **交叉引用修正**：spec/03 的「交互图谱（7.1）」旧章节号改为概念名引用（遵循 STYLE 规则）。
3. **spec/11-traceability.md**：新增「G×S 二维分级」机制行（初判 C / 核定 B/C 混合，命题本体获一手事件印证）。
4. **版本号同步**：VERSION / README（badge、状态行、APA 引用）/ CITATION.cff 统一为 v1.1.0。

## v1.0.0（2026-08-30）— 首次公开

本仓库为《AI 执行与监察体系》的对外公开版。内部文档经历了以下演进（v1 → v2.3.3），公开版重写为独立设计文档。

### 内部演进史（未公开的中间版本）

- **v1（2026-08-29 凌晨）**：初始设计。古代官制类比，一号执行 / 二号监察双角色，五类制度映射。
- **v2（2026-08-29 下午）**：收敛为可落地架构。五层结构 L0–L4、两条独立审计线、G0–G4 分级、信用分、交白卷出口、三号公证机、资源账本。
- **v2.1**：L1 责任主体拆分（厂商 40% / 部署方 15% / 行业 5%）、白卷率校准、强制力四锚点。
- **v2.2**：证据纪律。A/B/C/D 四级证据分级、保密分级（公开/受限/机密）、参考文献核验流程。
- **v2.3**：正面回应真实事故。证据完整性子系统 E（E1–E5）、蜂群治理（涌现检测、规模熔断）、内核级采集。
- **v2.3.1 – v2.3.3**：修订。证据分级修正（AIprint C 级、Sandlock B 级）、两起事故分列纠正、Anthropic 事件补充、25 条参考文献（其中 B 级 22 条）、三份一手材料链接补齐。

### 公开版关键决策（从内部文档到本仓库）

1. **第零章解散**：内部版本演进叙事不入库，改为面向外部读者的总纲与核心判断（`spec/01-principles.md`）。
2. **保密分级切割**：蜜罐样本构造、落地路线时间表、变更审阅记录移入 `restricted/`，公开仓库不包含。
3. **叙事重写**：全篇清除内部版本号（v1/v2/v2.1 等）与审阅编号（P0-1/2/3 等），改为「这个框架是什么、为什么这样设计、边界在哪」。
4. **证据升级**：附录 A 从 13 条扩至 25 条，全部含 DOI/arXiv 号；三份一手材料（OpenAI 博客 / 38 页技术报告 / METR-Redwood 调查）链接补齐。
5. **勘误**：OpenAI 技术报告页数 37 → 38。

### 发布前审查修复（2026-08-30，第三方审查报告）

1. **表格标准化**：全仓库 18 块表格去除 `[TABLE START/END]` 伪标记、补 `|---|` 分隔行（GitHub 可正常渲染）。
2. **许可证结构调整**：根 `LICENSE` 改为 CC BY-SA 4.0（纯文档仓库），Apache-2.0 移至 `LICENSE-CODE` 备用。
3. **证据数量统一**：25 条来源（A:1 / B:22 / C:1 / D:1），README / CHANGELOG / REFERENCES 注脚三处口径一致。
4. **单一真相源**：删除 `spec/14-references.md`，参考文献仅保留根 `REFERENCES.md`。
5. **事故数字勘误**：2026-07 事件统一为「约 1,200 个隔离 Agent 形成共享留言板（>70,000 条消息），其中约 700 个参与协同攻击」（与 OpenAI 38 页技术报告 / METR-Redwood 一致）。
6. **ANTITRUST 章节号**：第十三章 → 第十二章（`spec/12-enforcement.md`）。
7. **术语口径澄清**：`STYLE.md` 明确禁用内部审阅编号（P0-1/2/3），允许公开语义的优先级标记（P0/P1/P2）与未解问题编号（C12 等）。
8. **校验脚本**：新增 `scripts/verify_consistency.py`（18 项一致性检查）+ `.githooks/pre-commit`，防止此类矛盾再次进入仓库。

### README 门面修订（2026-08-30，吸收 5 家 AI 评审）

1. **tagline 更换**：「第一份委托契约」→「一份分层治理规范：面向 AI Agent 监察——证据分级、自校正、边界诚实」（+ English Abstract）。
2. **差异化前置**：能力表按独家性重排（G×S / 蜂群 / E 子系统 / 白卷 / 回灌前置，✨ 标注 5 项差异化）；新增架构概览 Mermaid 图。
3. **身份声明**：注明个人独立维护（@ZhangRui987），非企业项目。
4. **证据纠错流程**：补 14 天核验周期 + 维护者裁定 + 拒绝不静默关闭。
5. **口径校准**：「首个中文规范」→「面向中文读者」（避免被打脸）；D 级定义精修；「未解问题」→「诚实边界 + 研究选题」。
6. **入口完善**：快速开始补「5 分钟体验」+ 企业采购/合规方入口；与相关项目补 SWARM / AgentSight；新增「质量保障（自指验证）」小节；引用段补 APA 示例；Badge 区。
7. **外部评审事实纠错**：5 家评审中智谱 3 条质疑（事故无对应物 / #14 无链接 / ai-safe2 非开源）均基于过期信息，复核驳回（三份一手材料链接在库、#14 已解、ai-safe2 为开源双许可）。

### 证据口径勘误（2026-08-30，第二轮评审核验）

1. **Anthropic 2026-04 事件定级修正**：spec/06 与 spec/11 原定 A 级（「一手 / 可核查实证」），与 A 级定义（「真实环境复现的实验结果」）冲突——一手公开事件属 B 级。已统一降为 B（与 OpenAI/HF 事件同级），并补入 `REFERENCES.md` 独立条目。
2. **证据分布更新**：A:1（Sandlock，实验复现）/ B:23 / C:1（AIprint）/ D:1（Mitchell），合计 **26 条**。当前口径统一为「26 条参考文献（其中 B 级 23 条）」。README badge、分布声明、REFERENCES 注脚、校验脚本同步。

### 未决事项

15 条未解问题见 `spec/13-boundaries.md`，其中最重要的工程缺口是「去中心化蜂群（狼群）的反制手段缺失」。
